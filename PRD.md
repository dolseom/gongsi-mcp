# PRD — 기업집단 공시 특화 MCP 서버

> **프로젝트 코드명**: `gongsi-mcp`
> **한 줄 정의**: DART 전자공시 + 공정위 기업집단포털을 한 몸으로 묶어, **공시담당자가 과태료를 맞지 않게 만드는** MCP 서버
> **작성일**: 2026-07-28
> **버전**: v0.1 (초안)

---

## 0. 요약 (TL;DR)

| 항목 | 내용 |
|---|---|
| **무엇** | OpenDART 83개 API + 공정위 기업집단포털 15개 API를 **14개 MCP 도구**로 압축 |
| **누구** | 공시담당자, 기업집단 공시 실무자(공정거래팀), IR 담당자 |
| **왜 지금** | 2025년 공시위반 146건(5년 최다)·과태료 8.9억. 공정위가 지목한 원인이 **"담당자 업무 미숙"** |
| **왜 우리** | 조사된 DART MCP 5종 **전부** `pblntf_ty=J`(공정위공시)를 다루지 않음. 기업집단포털 API를 쓰는 MCP는 **전무** |
| **핵심 해자** | 단순 API 래퍼가 아닌 **공시의무 판정 룰 엔진**(법령·고시 코드화) |
| **스택** | TypeScript / Node.js 20+ / `@modelcontextprotocol/sdk` |
| **배포** | npm(`npx` 원클릭 setup) + Claude Code 플러그인 마켓플레이스 |

---

## 1. 배경과 문제 정의

### 1.1 시장에서 확인된 사실

2026년 5월 1일 기준 **공시대상기업집단 102개**(자산 5조 이상), 그중 **상호출자제한기업집단 47개**(자산 12조 이상)가 지정되었다. 2025년 기준으로도 92개 집단·3,301개 계열회사·232개 공익법인이 공시의무를 진다.

공정위의 2025년 공시이행 점검 결과:

- **위반 146건 — 최근 5년 중 최다**
- 49개 집단에 **과태료 총 8.9억원** 부과
- 과태료 상위: 한국앤컴퍼니(2,900만원), 삼성(2,000만원), 유진(2,000만원)
- 3년 연속 위반 상위 집단: 한국앤컴퍼니(28건), 태영(24건), 장금상선(21건), 한화(13건)
- **위반 유형 1위는 "지연 공시"이며, 공정위가 밝힌 원인은 "각 기업 공시담당자들의 업무 미숙"**
- 위반이 집중된 항목: **상품·용역거래(대규모내부거래)**, **임원·이사회 운영현황(기업집단현황공시)**

> 제품 관점에서 이 마지막 두 줄이 전부다. **문제는 데이터 부족이 아니라 판단 부족**이다. 담당자는 데이터를 못 찾아서 틀리는 게 아니라, "이 거래가 공시 대상인지", "기한이 언제까지인지"를 몰라서 늦는다.

### 1.2 담당자가 실제로 겪는 일 (문제 시나리오)

| # | 상황 | 현재 방식 | 소요 |
|---|---|---|---|
| P1 | 계열사와 80억 자금대여 건이 올라옴. 공시 대상인가? | 시행령 찾고 자본총계 확인하고 계산 | 30분~반나절, 오판 위험 |
| P2 | 이사회 의결이 내일. 공시 기한은 언제까지? | 고시 뒤져보거나 선배에게 질문 | 불확실 |
| P3 | 우리 집단 작년 공시 누락/지연 없었나? 공정위 점검 전 셀프체크 | DART에서 계열사 수십 개를 하나씩 수동 조회 | 며칠 |
| P4 | 타사는 이 항목을 어떻게 썼지? | DART에서 공시 찾아 HWP 다운로드해 열어봄 | 건당 10분 |
| P5 | 우리 집단 계열사 목록·순환출자 현황이 지금 어떻게 되지? | 기업집단포털 웹에서 화면 단위로 확인 | 반복 노동 |
| P6 | 대규모내부거래 연간 집계가 필요함 | 공시를 일일이 열어 엑셀로 옮김 | 수일 |

### 1.3 기존 솔루션의 공백

조사한 5개 DART MCP는 모두 **투자자·재무분석 관점**이며, 공시 규제 준수(compliance) 관점의 도구는 하나도 없다. 상세는 §2.

---

## 2. 경쟁 분석

### 2.1 DART MCP 경쟁 지형

| 프로젝트 | 스택 | 도구 | ★ | 타겟 | 공정위공시(J) | 기업집단포털 |
|---|---|---|---|---|---|---|
| **chrisryugj/korean-dart-mcp** | TS/npm | 15 | 87 | 개인투자자 | ❌ | ❌ |
| snaiws/DART-mcp-server | Python/Docker | ~8 | 11 | 일반 | ❌ | ❌ |
| keonho-kim/OpenDart-mcp | Python/Docker | 7 | 2 | 재무분석 | ❌ | ❌ |
| SongHyojun0228/opendart-mcp | TS/npm | 5 | 1 | 재무조회 | ❌ | ❌ |
| 2geonhyup/dart-mcp | - | - | - | 재무분석 | ❌ | ❌ |
| **gongsi-mcp (본 프로젝트)** | TS/npm | 11 | - | **공시담당자** | ✅ | ✅ |

**최강 경쟁자 `korean-dart-mcp` 분석**
- 강점: XBRL 완전 파싱(calculation linkbase 검증), HWP/PDF→마크다운(kordoc), 90일 자동분할, 22개 프리셋, SQLite FTS 회사명 해석, 애널리스트 프레임 3종(`insider_signal`, `disclosure_anomaly`, `buffett_quality_snapshot`)
- **명시 타겟**: "보유 종목 5-20개를 분기별 점검하는 중급 개미 투자자"
- **공백**: 공정위공시 전무. 기업집단 구조 전무. 규제 준수 관점 전무.
- **시사점**: 정면충돌이 아니라 **인접 시장**이다. 재무·XBRL은 이쪽이 앞서므로 우리는 그 영역을 "동등 수준으로 제공"하되 경쟁 축을 컴플라이언스로 옮긴다.

### 2.2 벤치마크 `korean-law-mcp` (2.3k★) 성공 요인 해부

| 요인 | 내용 | 우리 적용 |
|---|---|---|
| **API 압축** | 법제처 41개 API → **10개 도구**. v4.4.0에서 19개→10개로 통폐합해 "컨텍스트 52% 감축" | 98개 API → **11개 도구** |
| **고수준 태스크 도구** | `legal_research`(8 task) / `legal_analysis`(4 mode) — 저수준 조회가 아닌 **상황 단위** | `check_disclosure_duty`, `audit_group_disclosures` |
| **킬러 = 검증** | "LLM 환각 실시간 탐지"(인용 검증)를 1번 피처로 | **"공시 누락·지연 탐지"**를 1번 피처로 |
| **메타 도구** | `discover_tools` / `execute_tool`로 전문 도구를 숨기고 필요 시 노출 | v1.1에서 도입 |
| **3단 배포** | 원격 엔드포인트 / npx setup 마법사 / 플러그인 마켓플레이스 | npx + 마켓플레이스 (원격은 v2) |
| **README 전략** | 실무 페인포인트 시나리오, 영상 데모, Before/After, 배지, 버전 이력 서사 | §9 참조 |

> **가장 중요한 교훈**: 도구를 API 단위로 만들면 안 된다. **담당자의 질문 단위**로 만들어야 한다.

---

## 3. 타겟 사용자

### 3.1 1차 페르소나 — "공시담당 김대리" (핵심)

- 소속: 공시대상기업집단 소속 계열사 재무팀 / 지주사 공정거래팀
- 업무: 대규모내부거래 공시, 기업집단현황공시, 비상장사 중요사항공시
- 공포: 공정위 연례 점검에서 우리 회사 이름이 나오는 것
- 현재 도구: DART 웹, 기업집단포털 웹, 엑셀, 공정위 매뉴얼 PDF
- **핵심 니즈**: "이거 공시 대상 맞아요?" 를 5초 안에, 근거 조문과 함께

### 3.2 2차 페르소나 — "지주사 공정거래팀 박차장"

- 업무: 집단 전체 공시 총괄, 공정위 점검 대응, 계열사 공시 교육
- **핵심 니즈**: 집단 전체 공시 이행 현황 일괄 점검, 지연 패턴 사전 발견

### 3.3 3차 페르소나 — "IR 담당 이과장"

- 업무: 투자자 대응, 경쟁사 공시 모니터링, 지배구조 자료 작성
- **핵심 니즈**: 타사 공시 사례 검색, 기업집단 구조 자료 생성

### 3.4 명시적 비타겟

- 단타 트레이더, 퀀트 백테스터 → `korean-dart-mcp` 등 기존 도구가 적합
- 재무제표 시계열 대량 수집 → OpenDartReader / dart-fss가 적합

---

## 4. 데이터 소스 인벤토리

### 4.1 OpenDART API (83개, 무료, 일 20,000건)

| 그룹 | 개수 | 내용 |
|---|---|---|
| DS001 공시정보 | 4 | 공시검색(2019001), 기업개황(2019002), 공시서류원본파일(2019003), 고유번호(2019018) |
| DS002 정기보고서 주요정보 | 30 | 주식총수, 자기주식, 배당, 증자(감자), 채무증권, CP/단기사채/회사채/신종자본증권/조건부자본증권 미상환잔액, 공모·사모자금 사용내역, 감사인·감사의견, 감사·비감사용역, 사외이사, 최대주주(변동), 소액주주, 임원, 직원, 미등기임원 보수, 이사·감사 보수(4종), 개인별 보수(5억 이상, Ver1/Ver2), 상위5인(Ver1/Ver2), **타법인 출자현황** |
| DS003 재무정보 | 7 | 단일회사 주요계정(2019016), 다중회사 주요계정(2019017), 단일회사 전체 재무제표(2019020), 단일/다중회사 주요 재무지표(2022001/2022002), XBRL 원본(2019019), XBRL 택사노미(2020001) |
| DS004 지분공시 | 2 | 대량보유 상황보고(5%룰, 2019021), 임원·주요주주 소유보고(2019022) |
| DS005 주요사항보고서 | 36 | 부도, 영업정지, 회생절차, 해산, 유상·무상·유무상증자, 감자, 관리절차 개시/중단, 소송, 해외상장(4종), CB/BW/EB 발행, 조건부자본증권, 자산양수도·풋백옵션, **타법인주식 양수/양도**, 유형자산 양수/양도, 영업양수/양도, 자기주식 취득/처분/신탁 체결·해지, 합병, 분할, 분할합병, 주식교환·이전, 주권관련사채권 양수/양도 |
| DS006 증권신고서 | 6 | 지분증권, 채무증권, 증권예탁증권, 합병, 분할, 주식의포괄적교환·이전 |

**공시유형 코드 (`pblntf_ty`)**
`A`정기 `B`주요사항 `C`발행 `D`지분 `E`기타 `F`외부감사 `G`펀드 `H`자산유동화 `I`거래소 **`J`공정위** ← 미개척

**공정위공시 상세유형 (`pblntf_detail_ty`) — 본 프로젝트의 핵심 자산**

**✅ M0 실측 검증 완료 (2026-07-28, 조회기간 2026-06-01~07-28)**

| 코드 | 명칭 | 근거 | 실측 건수 | 상태 |
|---|---|---|---|---|
| `J001` | 대규모내부거래관련 | 공정거래법 §26 | **1,555** | ✅ 활성 |
| `J004` | 기업집단현황공시 | 공정거래법 §28 | **1,846** | ✅ 활성 |
| `J005` | 비상장회사중요사항공시 | 공정거래법 §27 | **234** | ✅ 활성 |
| `J008` | 대규모내부거래(공익법인) | 공정거래법 §29 | **17** | ✅ 활성 |
| `J009` | 하도급대금결제조건공시 | 하도급법 §13-3 | **73** | ✅ 활성 |
| `J002` `J003` `J006` `J007` | 구제도·미사용 | - | 0 | ⬜ 비활성 |

> **실무상 유효 코드는 5개**(J001·J004·J005·J008·J009)다. 구현은 이 5개에 집중한다.

**실측으로 확인된 3가지 설계 함의**

1. **`report_nm`만으로 상당한 구조화가 가능하다.** 원문 파싱 없이도 공시 세부유형을 분류할 수 있어 R2(파싱 실패) 리스크가 크게 낮아진다.
2. **`[기재정정]` 접두사가 `report_nm`에 들어있다.** 정정공시 탐지가 문자열 판정으로 가능하다.
3. **대상 법인이 대부분 `corp_cls="E"`(기타법인=비상장)다.** 기존 DART MCP들이 다루는 상장사 영역과 모집단이 거의 겹치지 않는다 — 타겟 분리가 데이터로 확인됐다.

**J001 세부 서식 분류 (실측 300건, 44종)** — 룰 엔진 `transaction_type` enum의 실제 근거

| 대분류 | 서식명(report_nm) 예시 |
|---|---|
| 자금 | 특수관계인으로부터자금차입(52), 특수관계인에대한자금대여(19) |
| 유가증권 | 특수관계인과의수익증권거래(18), 주식의취득/처분, 채권매수, 유가증권차입, 사모사채발행의결 |
| 자산 | 특수관계인에대한담보제공(13), 특수관계인으로부터받은담보(10), 부동산임차, 증여 |
| 상품·용역 | 동일인등출자계열회사와의상품ㆍ용역거래(4) / 거래변경(23) |
| 출자 | 특수관계인에대한출자(10), 특수관계인의유상증자참여(8) |
| **약관 금융거래** ★ | 계열금융회사의약관에의한금융거래-[유가증권-채권/수익증권/주식/기타], [예ㆍ적금], [보험], [장단기대여], [장단기차입], [파생금융상품], [CP] / 약관에의한금융거래시계열금융회사의거래상대방의공시 / 계열금융회사를거래상대방으로한단기금융상품거래의분기별공시 |

> ★ **예상 못 했던 큰 축**: 시행령 §33④(약관에 의한 금융업 일상거래)에 근거한 **분기별 공시** 계열이 J001의 상당 비중을 차지한다. 금융계열사를 보유한 집단에게는 이쪽이 오히려 주업무다. 룰 엔진에서 별도 트랙으로 다룬다.

**J005 세부 서식 분류 (실측 200건, 30종)** — 시행령 §34 항목과 정확히 대응

유상증자결정(36) · 최대주주등의주식보유변동(32) · **비유동자산**취득결정(29)/처분결정(10) · 타인을위한채무보증결정(22) · 타법인주식및출자증권취득(10)/처분결정 · 감자결정(8) · 회사합병결정(7) · 주요주주의주식보유변동(7) · 해산사유발생(5) · 타인을위한담보제공결정(4) · 회생절차개시결정(3) · 증여결정(2) · 타인에의한당사의채무면제 · 전환사채발행결정 · 영업양도/양수결정 · 무상증자결정 · 주식교환ㆍ이전결정

> 시행령의 "고정자산"이 실무 서식에서는 **"비유동자산"**으로 표기된다. 용어 매핑 테이블 필요.

**J004 정정률 — 제품 기회의 발견**

실측 200건 중 **182건(91%)이 `[기재정정]`**이었다. 원공시는 18건뿐이다.

> 기업집단현황공시는 **정정이 예외가 아니라 일상**이다. 담당자가 한 번에 제대로 쓰기가 그만큼 어렵다는 실증이며, §5A.2 `draft_disclosure`(초안 생성)와 §5A.4(정정 리스크 예보)의 직접적 근거가 된다.

서식 구분도 확인됐다: `[연1회공시및1/4분기용(개별회사)]`, `[연1회공시및1/4분기용(대표회사)]`, `[연1회(동일인용)]` — **개별회사/대표회사/동일인용 3종**으로 나뉜다.

### 4.1.1 공시 원문 XML 구조 실측 — R2 리스크 해소

공시서류원본파일 API(2019003)로 J001·J004 원문을 받아 구조를 확인했다.

**공통 구조** (ZIP → 단일 XML, **UTF-8**, `dart4.xsd`)

```xml
<DOCUMENT>
  <DOCUMENT-NAME ACODE="80734">특수관계인에대한담보제공</DOCUMENT-NAME>
  <FORMULA-VERSION ADATE="20240129">6.0</FORMULA-VERSION>
  <COMPANY-NAME AREGCIK="00974875">(주)소노스테이션</COMPANY-NAME>
  <BODY><SECTION-1><TABLE>…정형화된 표…</TABLE></SECTION-1></BODY>
</DOCUMENT>
```

| 요소 | 실측값 | 활용 |
|---|---|---|
| `ACODE` | 80734(담보제공) / 80718(자금차입) / 80622(집단현황 연1회 대표회사용) | **서식별 파서 매핑 키** |
| `FORMULA-VERSION` | 6.0(2024-01-29), 6.2(2026-04-27) | 서식 개정 추적 → **버전별 파서 분기 필요** |
| `AREGCIK` | corp_code | 회사 식별 (resolve 불필요) |
| 인코딩 | **UTF-8** | EUC-KR 변환 불필요 |

**J001 필드 실측 — 판정에 필요한 값이 원문에 전부 있다**

| 서식 | 항목 구조 |
|---|---|
| 담보제공(80734) | 〔헤더〕기업집단명·회사명·**공시일자**·관련법규(공정거래법 제26조) → 1.거래상대방+**회사와의 관계** → 2.담보제공 내역(가~아, **사.거래상대방 총잔액**) → **3.이사회 의결일** → 사외이사·감사 참석여부 → 4.기타 → ※관련공시일 |
| 자금차입(80718) | 1.차입유형 → 2.차입 내역(가.계약체결일~바.상환방법) → 3.자금용도 → **4.거래상대방과의 차입총계** → **5.이사회 의결일** → **6.해당회사의 직전사업연도말 자산총액** → 7.기타 |

> **확정 3가지**
> ① **이사회 의결일 + 공시일자가 모두 원문에 존재** → **지연 판정 완전 자동화 가능**. 실검증: 의결 2026-07-22 → 공시 2026-07-28, 비상장 **7영업일** 기한 내 **적법** 판정
> ② **총거래잔액이 원문 필드다** → `running_balance`는 별도 계산 없이 파싱만으로 구현
> ③ **직전사업연도말 자산총액도 서식에 포함** → 기준금액 판정 근거가 공시 자체에 들어있다
>
> ⚠️ **단위가 "백만 원"** — 금액 파싱 시 단위 변환 필수 (`231,600` = 2,316억원)
> ⚠️ **서식마다 항목 번호·명칭이 다르다** → **44종 필드 매핑 테이블**이 필요하다. 작업량은 있으나 `ACODE`로 분기하면 확실히 가능하다

**J004 실측 — 대용량 + 시행령 §35② 정확 대응**

`기업집단현황공시(연1회-대표회사용)` ACODE=80622, 서식 v6.2, **523,991 bytes**.

정정공시는 본문 앞에 **정정 헤더 블록**이 붙는다:
`1.정정대상 공시서류 / 2.정정대상 공시서류의 최초제출일 / 3.정정사항`
→ **정정 이력·사유 추적이 원문 파싱으로 가능** (§5.15 `amendment_analysis`의 구현 근거)

본문 7개 대항목 (= 시행령 §35② 대응):

1. 일반현황
2. 임원, 이사회 등의 운영현황 **[ESG]** — 가.이사회 운영 / 나.위원회 설치·운영 / 다.주총 의결권행사 제도 / 라.소수주주권 행사내용
3. 주식소유현황 **[ESG]**
4. 순환출자 현황 **[ESG]**
5. 지주회사 현황 **[ESG]**
6. 금융ㆍ보험사 의결권 행사 현황 **[ESG]**
7. 계열회사와 특수관계인간 거래현황 — 가.일반차입/나.한도약정차입/다.리스부채 · 가.유가증권 매도/나.매입/다.주식지급약정 · **가.상장회사 상품ㆍ용역거래(분기)** / **나.비상장회사 상품ㆍ용역거래(연1회)**

> **예상 못 한 발견 — `[ESG]` 태그**: 5개 항목에 `[ESG]` 표시가 붙어 있다. 기업집단현황공시 항목이 ESG 공시 자료로도 활용된다는 뜻이며, **IR 담당자에게 추가 가치**가 된다. → `benchmark_peers`에 ESG 축 추가 검토.
>
> 524KB 대용량이므로 `read_disclosure`는 **섹션 선택 조회**와 `format:"tables"`가 필수다. 전문을 통째로 컨텍스트에 넣으면 토큰이 폭발한다.

### 4.2 공정위 기업집단포털 OpenAPI (15개, 공공데이터포털, 무료, 개발계정 일 10,000건)

**어떤 경쟁 MCP도 사용하지 않는 미개척 데이터셋.**

| # | API | 활용 |
|---|---|---|
| 1 | 지정된 대규모기업집단 조회 | 집단 목록·지정 이력 |
| 2 | 지정된 대규모기업집단 자산순위 조회 | 순위·상출집단 여부 |
| 3 | 지정된 대규모기업집단 소속회사 조회 | **계열사 전수 목록** |
| 4 | 소속회사 개요 정보 조회 | 대표자·설립일·법인등록번호 |
| 5 | 소속회사 임원현황 정보 조회 | 겸직·임원 네트워크 |
| 6 | 소속회사 주주현황 정보 조회 | 지분구조 |
| 7 | 소속회사 참여업종 정보 조회 | 업종·대표상품 |
| 8 | 소속회사 재무현황 정보 조회 | **자본금·자본총계 → 공시기준액 계산의 입력값** |
| 9 | 지주회사 설립·전환 신고현황 | 지주 체제 여부 |
| 10 | 지주회사 자회사·손자회사 현황 | 수직 구조 |
| 11 | 내부지분(대동비교) 현황 | 내부지분율 추이 |
| 12 | 특수관계인 내부지분 현황 | **동일인·친족 지분 → §26 특수관계인 판정 보조** |
| 13 | 기업집단별 순환출자 현황 | 순환출자 고리 |
| 14 | 계열 편입/제외/유예 변경내역 | **계열편입일 → 공시의무 발생 시점** |
| 15 | 사용 가능 공개년월 조회 | 데이터 기준월 확인(선행 호출 필수) |

**✅ M0 실측 완료 (2026-07-29) — 4개 API 실호출 성공**

**엔드포인트 (공정위 기관코드 `1130000`, 패턴 `/{서비스명}/{서비스명}Api`)**

| API | 서비스명 | 필수 파라미터 |
|---|---|---|
| 사용가능 공개년월 | `publicYmList` | `jobSeCode`(지정=0001/지주=0003), `presentnYear` |
| 지정 대규모기업집단 | `appnGroupSttusList` | `presentnYear`(예 `202605`) |
| 소속회사 조회 | **`appnGroupAffiList`** | `presentnYear`, `unityGrupCode` |
| 소속회사 재무현황 | **`financeCompSttusList`** | `presentnYear`, `unityGrupCode`, `jurirno` |

> ⚠️⚠️ **`User-Agent` 헤더가 필수다.** 헤더 없이 호출하면 게이트웨이가 **HTTP 403 `Forbidden`**(XML 에러가 아닌 순수 텍스트)을 반환한다. 브라우저 UA를 세팅하면 즉시 200이 된다. **키·활용신청 문제로 오진하기 쉬운 함정이므로 HTTP 클라이언트 기본 헤더에 반드시 포함한다.**

**실측 응답 (2026-07-29)**

```
appnGroupSttusList(202605) → totalCount=102, 상호출자제한집단 47
  unityGrupNm=삼성 unityGrupCode=K1000032 smerNm=이재용
  repreCmpny=삼성전자(주) sumCmpnyCo=67 invstmntLmtt=상호출자제한집단

appnGroupAffiList(K1000032) → totalCount=67
  entrprsNm=(주)멀티캠퍼스  jurirno=1101111960792  bizrno=1048153114
  rprsntvNm=안재우  fondDe=20000504  grinil=20000701   ← grinil=계열편입일

financeCompSttusList(jurirno=1301110006246) → 삼성전자
  assetsTotamt=358,902,051,000,000   caplTotamt=254,330,083,000,000
  caplAmount=897,514,000,000         debtTotamt=104,571,968,000,000
  selngAmount=238,043,009,000,000    thstrmNtpfAmount=33,686,601,000,000
  stacntDudt=20251231                ← 단위: 원
```

> **§5.1 기준금액 로직이 실데이터로 검증됐다**:
> `min(100억, max(5억, max(자본총계 254.3조, 자본금 8,975억) × 5% = 12.7조)) = **100억**`

- **공개년월이 전부 `05`월**(202605, 202505, 202405…)로 확인 → 아래 연 1회 갱신이 데이터로 입증됨
- ⚠️ **갱신주기는 "실시간"이 아니라 연 1회다.** 매년 2~4월 자료 제출·검토를 거쳐 **5월 1일 기준 공개**.
  - 설계 영향 ①: 캐시 TTL **1년**, 매년 5월 초 강제 무효화 (당초 30일 계획 수정)
  - 설계 영향 ②: **연중 계열편입·제외 미반영.** 감사 도구는 포털 스냅샷(5/1)과 DART 공시(실시간)의 시점 차를 보정해야 한다. `grinil`(계열편입일)로 일부 보정 가능. → R9

**✅ R1(최대 리스크) 원리적 해소**

DART 기업개황 API가 `jurir_no`(13자리 법인등록번호)를 반환함을 실측 확인했다.

```
삼성전자(00126380)     → jurir_no "1301110006246"
소노스테이션(00974875) → jurir_no "1101114488816"   ← 비상장 기타법인도 제공
```

기업집단포털도 법인등록번호를 제공하므로 **법인등록번호를 조인 키로 사용 가능**하다. 남은 과제는 실제 매핑률 측정.

> **호출량 설계**: DART 전체 11.6만 건을 `corp_code`별로 조회하면 일 한도(20,000)로 6일이 걸린다. 대신 **①고유번호 API(`corpCode.xml`)로 전체 목록(corp_code+corp_name) 일괄 확보 → ②회사명으로 후보 압축 → ③후보만 `company.json` 호출해 `jurir_no`로 확정**한다. 계열사 약 3,300개 규모면 하루 한도 안에서 처리된다.

> **결합의 가치**: DART는 "무엇을 공시했는가"(이벤트)만 안다. 기업집단포털은 "누가 공시해야 하는가"(모집단)를 안다. **둘을 합쳐야 비로소 "누가 공시해야 했는데 안 했는가"를 계산할 수 있다.** 이것이 §5.2 `audit_group_disclosures`의 원리이며, 단일 소스로는 원천적으로 불가능하다.

### 4.3 법령·고시 (룰 엔진의 근거)

| 규범 | 내용 |
|---|---|
| 공정거래법 §26 / 시행령 §33 | 대규모내부거래 이사회 의결 및 공시 |
| 공정거래법 §27 / 시행령 §34 | 비상장회사 등의 중요사항 공시 |
| 공정거래법 §28 / 시행령 §35 | 기업집단현황 등에 관한 공시 |
| 공정거래법 §29 / 시행령 §36 | 특수관계인인 공익법인의 이사회 의결 및 공시 |
| 하도급법 §13-3 | 하도급대금 결제조건 공시 |
| 고시 | 대규모내부거래 등에 대한 이사회 의결 및 공시에 관한 규정 |
| 고시 | 공시대상기업집단 소속회사 등의 중요사항 공시에 관한 규정 |
| 고시 | 기업집단현황 등에 관한 공시 규정 |
| 고시 | 대규모내부거래 등 공시의무 위반사건 과태료 부과기준 |

---

## 5. 제품 설계 — MCP 도구 11종

### 설계 원칙

1. **도구는 API가 아니라 질문 단위로 만든다.** 98개 API → 11개 도구.
2. **컨텍스트 예산을 지킨다.** 전체 도구 설명 합계 ≤ 6,000 토큰. 상세 파라미터는 enum + 짧은 설명으로.
3. **모든 판정에는 근거를 붙인다.** 판정 결과에는 반드시 법령 조문·계산식·원천 데이터를 함께 반환한다. (환각 방지 = korean-law-mcp의 신뢰 전략)
4. **모르면 모른다고 반환한다.** 자본총계 미확보 시 추정하지 않고 `insufficient_data`와 필요 입력을 명시.

---

### 5.1 `check_disclosure_duty` ★ 킬러 #1 — 공시의무 진단·기한 계산

> **P1·P2 해결.** 거래 정보를 넣으면 공시 대상 여부·근거·기한을 판정한다. 이 도구 하나가 제품의 존재 이유다.

```ts
{
  duty: "large_internal_transaction" | "unlisted_material" | "group_status"
      | "public_interest_corp" | "subcontract_payment" | "auto",
  company: string,              // 회사명 또는 corp_code
  transaction?: {
    type: "fund" | "securities" | "asset" | "goods_services",
    counterparty: string,       // 거래상대방
    counterparty_relation?: "affiliate" | "same_person" | "relative" | "other",
    amount: number,             // 원 단위
    board_date?: string,        // 이사회 의결(예정)일 YYYYMMDD
    is_quarterly_aggregate?: boolean  // 상품·용역: 분기 합계 여부
  },
  event?: {                     // 비상장사 중요사항용
    type: "fixed_asset" | "other_corp_stock" | "gift" | "guarantee"
        | "debt_relief" | "capital_change" | "cb_bw" | "merger" | "dissolution",
    amount?: number,
    occurred_date?: string
  },
  capital?: { total_equity?: number, paid_in_capital?: number, total_assets?: number },
  // 미입력 시 DART 재무 API + 기업집단포털 재무현황에서 자동 조회
  as_of?: string                // 판정 기준일(기본: 오늘)
}
```

**반환**

```ts
{
  verdict: "required" | "not_required" | "insufficient_data",
  threshold: {
    applied: number,            // 적용된 기준금액
    formula: string,            // "min(100억, max(자본총계 1,200억, 자본금 500억) × 5% = 60억) = 60억"
    inputs: { total_equity, paid_in_capital, source: "DART 2019016 | 기업집단포털 API#8 | 사용자입력" }
  },
  deadline: {
    date: string,               // "20260805"
    rule: string,               // "상장회사: 이사회 의결 후 3영업일 이내 (고시 제6조①)"
    business_day_adjusted: boolean,
    days_remaining: number
  },
  board_resolution_required: boolean,
  legal_basis: [{ law: "공정거래법 제26조제1항", text: string, url: string }],
  filing_form: string,          // 제출 서식명
  checklist: string[],          // 공시서류 기재 필수항목
  warnings: string[]            // "동일 상대방 동일 유형 총거래잔액 합산 확인 필요" 등
}
```

**룰 엔진 로직 — 대규모내부거래 (시행령 §33①)**

```
기준금액 = min(
  100_0000_0000,                                    // 1호: 100억원
  max(5_0000_0000,                                  // 2호 단서: 최소 5억
      max(자본총계, 자본금) × 0.05)
)
공시대상 = 거래금액 ≥ 기준금액
```

> 💡 흔히 통용되는 "50억 기준"은 **폐지된 옛 기준**이다. 현행은 100억. 이 오류를 잡아주는 것만으로도 도구의 값어치가 있다.

**거래유형 4종 (법 §26①)**: ①자금(가지급금·대여금) ②유가증권(주식·회사채) ③자산(부동산·무체재산권) ④**상품·용역**(계열회사 대상, **분기 합계액**으로 산정) — 위반이 가장 많은 유형이 ④이므로 분기 누적 합산을 명시적으로 지원한다.

**공시기한 (고시 제6조① 원문 확인 — 2026-07-29 정정)**

| 구분 | 기한 |
|---|---|
| **상장회사** | 이사회 의결 후 **3영업일 이내** |
| **비상장회사·공익법인** | 이사회 의결 후 **7영업일 이내** |

> ⚠️ **정정 이력**: 초안에 "상장 1일 / 비상장 7일"로 적었으나 **오류**였다. 웹 자료에 널리 퍼진 "1일 이내"는 사실과 다르며, 고시 원문은 **3영업일**이다. 또한 "일"이 아니라 **"영업일"**이다.
>
> **영업일 정의 (고시 제2조⑧)**: 공휴일, **토요일**, 「근로자의 날 제정에 관한 법률」에 따른 **근로자의 날(5/1)** 을 제외한 날. → 영업일 캘린더에 근로자의 날을 반드시 포함해야 한다.
>
> 만료일이 해당 회사의 영업일이 아니면 **다음 최초 영업일**까지 (고시 제6조②).

**자본총계·자본금의 기준시점 (고시 제2조③ — 경쟁자가 놓치는 지점)**

- **자본총계** = **주주총회에서 승인된 최근 사업연도말 재무제표**에 표시된 자본총계
- **자본금** = **이사회 의결일의 직전일**의 자본금
- 공익법인: 자본총계→**순자산총계**, 자본금→**기본순자산**

**거래금액 산정 기준 (고시 제4조③) — 단순 거래금액이 아니다**

| 거래유형 | 산정 기준 |
|---|---|
| 자금·유가증권·자산 | 실제 거래금액 |
| **담보제공** | **담보한도액** (담보금액 아님) |
| **부동산임대차** | 연간임대료 + 보증금을 「부가가치세법 시행규칙」 §47 이율로 환산한 연간임대료의 **합산액** |
| **보험계약** | **보험료총액** |
| 상품·용역 | **분기 거래금액 합계액** |

> 실측한 담보제공 공시(ACODE 80734)에 `마.담보한도 231,600` / `바.담보금액 4,700`이 나란히 있었다. **판정에 쓰는 값은 담보한도**다. 이걸 틀리면 판정이 통째로 뒤집힌다.

**판단 단위 (고시 제4조③)**: 자금·유가증권·자산은 **동일 거래상대방 + 동일 거래대상** 기준, 상품·용역은 **동일 거래상대방** 기준.

**대규모내부거래에서 제외 (고시 제4조⑥)**: ①권리행사·의무이행에 따른 부수적 거래로 새로운 거래관계가 성립하지 않는 행위 ②거래당사자가 거래금액·단가·이자율 등 거래조건을 결정할 수 없는 행위

**주요내용 변경 시 재의결·재공시 (고시 제8조②)**: 거래목적/대상 변경, 거래상대방 변경(상호변경·합병 등 실질 동일 시 제외), **거래금액·단가·약정이자율이 당초 대비 20% 이상 증감**, 계약기간 등 중대 변경. 계약서상 **자동연장조항에 따른 연장도 이사회 의결 필요**(제4조⑤).

**비상장사 중요사항 (시행령 §34)**

| 조건 | 임계 |
|---|---|
| 대상회사 | 직전사업연도말 자산총액 100억 이상 / 또는 100억 미만이어도 특수관계인 지분 20% 이상(및 그 회사가 50% 초과 보유한 회사). 청산·1년 이상 휴업 제외 |
| 고정자산 취득·처분 | 자산총액의 **10%** 이상 |
| 타법인(계열 제외) 주식·출자증권 취득·처분 | 자기자본의 **5%** 이상 |
| 증여(수증 포함) | 자기자본의 **1%** 이상 |
| 타인 위한 담보제공·채무보증 | 자기자본의 **5%** 이상 |
| 채무 면제·인수 | 자기자본의 **5%** 이상 |
| 증자·감자 / CB·BW 발행 | 결정 시 (금액 무관) |
| 최대주주·주요주주 지분변동 | 발행주식총수의 **1%** 이상 변동 |
| 기한 | 사유 발생일부터 **7영업일 이내** (고시 제5조의2④). 단 **주요주주 주식보유비율 변동은 분기 공시** |
| 자기자본 특례 | **자기자본이 자본금에 미달하면 자기자본 = 최근 자본금**으로 본다 (고시 제5조의2③) |
| 대상 제외 | **금융업·보험업 영위 회사 제외** (고시 제2조②) — 시행령에는 없고 고시에만 있다 |
| 중복 배제 | 법 §26(대규모내부거래)로 공시되는 사항은 제외. 자본시장법 공시로 갈음 가능(공정거래법 사항임을 표시) |
| 사유 발생일 | "결정이 있는 경우" = **이사회 결의(위원회 결의 포함) 또는 대표이사 등 사실상 권한자의 결정이 있은 때** (고시 제5조의2⑤) |

> 자산총액·자기자본 기준시점: 매 사업연도 종료 후 3개월이 지난 날 ~ 다음 사업연도 종료 후 3개월이 되는 날. 신설법인은 설립 시 납입자본금 기준. (이 기준시점 규칙까지 구현해야 정확한 판정이 된다 — 경쟁자가 흉내내기 어려운 지점)

**공익법인 (시행령 §36)**: 대규모내부거래와 동일한 `min(100억, max(5억, max(자본총계,자본금)×5%))` 기준.

**기업집단현황공시 (시행령 §35)**: 대상은 자산총액 100억 미만 + 청산/1년 이상 휴업 회사를 **제외한 모든 소속회사**. 공시항목 10종(일반현황·주주현황·출자현황·지주회사등 아닌 계열사·상호출자·순환출자·채무보증·금융보험사 의결권행사·특수관계인 거래현황·계열사 상품용역 거래내역). 마지막 항목은 **사업기간 매출액의 5% 이상 또는 50억원 이상**일 때 작성의무 발생(상장사=분기, 비상장사=연).

> ✅ **전부 원문으로 확정 완료**: 분기별 공시 기준일(분기 종료 후 2개월)·연1회 공시 기한(5/31)은 고시 §5② 원문으로, 하도급대금 결제조건 공시는 **하도급법 시행령 §8의2② 원문**("매 반기가 끝난 날의 다음 날부터 45일 이내", 상반기분 ~8/14·하반기분 ~익년 2/14)으로 확정. 공정위 「하도급대금 결제조건 공시제도 가이드라인」(2026.5)과도 일치.

---

### 5.2 `audit_group_disclosures` ★ 킬러 #2 — 집단 공시 이력 감사

> **P3 해결.** 공정위가 하는 점검을 우리가 먼저 돌린다. **DART + 기업집단포털 결합이 없으면 구현 불가능한 도구.**

```ts
{
  group: string,                // 기업집단명 또는 기업집단코드
  period: { from: string, to: string },   // YYYYMMDD
  scope?: ("large_internal_transaction" | "unlisted_material"
         | "group_status" | "public_interest_corp" | "subcontract")[],
  include_affiliates?: string[],   // 특정 계열사만
  detect?: ("missing" | "delayed" | "amended" | "anomaly")[],
  output?: "summary" | "detailed" | "table"
}
```

**동작 원리**

1. 기업집단포털 API#3으로 **계열사 전수 목록** 확보 (= 공시의무 모집단)
2. API#14로 **계열편입/제외일** 확보 → 각 사의 의무 발생·소멸 구간 계산
3. 각 계열사 `corp_code` 해석 (기업집단포털 법인등록번호 ↔ DART corp_code 매핑)
4. DART 공시검색으로 기간 내 `pblntf_ty=J` 공시 전수 수집 (90일 자동분할)
5. 대사(reconciliation):
   - **누락 후보**: 기업집단현황공시 의무가 있는데 해당 분기 J004 공시가 없는 회사
   - **지연 후보**: 이사회 의결일(원문 파싱) 대비 접수일이 기한 초과
   - **정정 이력**: 동일 건 정정공시 반복 → 품질 리스크 신호
   - **이상 패턴**: 특정 계열사·특정 항목 집중, 마감일 당일 몰림

**반환**

```ts
{
  group: { name, code, designated_year, rank, is_mutual_investment_restricted },
  coverage: { affiliates_total: number, affiliates_with_duty: number, checked: number },
  findings: [{
    severity: "high" | "medium" | "low",
    type: "missing" | "delayed" | "amended" | "anomaly",
    company: string,
    duty: string,
    expected: { deadline: string, basis: string },
    actual: { rcept_no?: string, rcept_dt?: string, delay_days?: number },
    evidence_url: string,
    note: string
  }],
  stats: { by_type, by_company, by_quarter, delay_distribution },
  disclaimer: "본 결과는 공개데이터 기반 추정이며 공정위 공식 판단이 아님"
}
```

> **면책 설계 필수**: "누락"은 우리가 관측 못 한 것일 수 있다(면제 사유, 별도 제출 등). 반드시 **"후보(candidate)"** 로 표기하고 확인 방법을 함께 제시한다. 과잉 확신은 이 제품의 최대 리스크다(§11).

---

### 5.3 `get_group_structure` ★ 킬러 #3 — 기업집단 구조 조회

> **P5 해결.** 기업집단포털 15개 API를 하나의 도구로.

```ts
{
  group: string,
  view: "affiliates" | "ownership" | "circular" | "holding"
      | "internal_share" | "changes" | "overview",
  year_month?: string,          // 미지정 시 API#15로 최신 공개년월 자동 조회
  company?: string,             // 특정 계열사로 한정
  format?: "table" | "tree" | "mermaid" | "raw"
}
```

| view | 결합 API | 산출 |
|---|---|---|
| `overview` | #1,#2,#3 | 집단 개요·자산순위·상출집단 여부·계열사 수 |
| `affiliates` | #3,#4,#7 | 계열사 전수 + 대표자·설립일·업종 |
| `ownership` | #6,#12 | 주주현황 + 특수관계인 내부지분 |
| `circular` | #13 | 순환출자 고리 (mermaid 그래프 출력) |
| `holding` | #9,#10 | 지주회사 → 자회사 → 손자회사 트리 |
| `internal_share` | #11 | 내부지분율 추이 |
| `changes` | #14 | 계열편입·제외·유예 이력 |

**mermaid 출력**: 지배구조/순환출자를 다이어그램으로 반환 → 사용자가 그대로 보고서에 붙여넣기. (Artifact·문서화 친화)

---

### 5.4 `track_internal_transactions` ★ 킬러 #4 — 대규모내부거래 모니터링

> **P6 해결.** J001/J008 공시를 파싱해 구조화된 집계로.

```ts
{
  target: string,               // 회사명 | 기업집단명
  target_type?: "company" | "group",
  period: { from: string, to: string },
  transaction_type?: ("fund" | "securities" | "asset" | "goods_services")[],
  counterparty?: string,        // 특정 상대방 필터
  group_by?: "counterparty" | "type" | "quarter" | "company",
  min_amount?: number,
  include_public_interest_corp?: boolean   // J008 포함
}
```

**반환**: 거래 건별 리스트(일자·상대방·유형·금액·이사회의결일·접수번호) + 집계(상대방별/유형별/분기별 합계) + **총거래잔액 추이** + 이상 신호(분기말 집중, 기준금액 직하 반복 = 회피 의심).

> "기준금액 바로 아래 금액의 반복 거래"는 공정위도 주목하는 패턴이다. 자사 셀프체크용으로도, 타사 분석용으로도 쓰인다.

---

### 5.5 `search_disclosures` — 공시 검색 (공정위 프리셋 내장)

```ts
{
  query?: string,               // 회사명/집단명
  corp_code?: string,
  date_from?: string, date_to?: string,
  preset?: "ftc_all" | "internal_transaction" | "group_status" | "unlisted_material"
         | "subcontract" | "public_interest_corp"        // 공정위 프리셋 ★
         | "annual_report" | "material_event" | "merger" | "treasury_stock"
         | "capital_increase" | "cb_bw" | "audit_opinion" | ...,  // 일반 프리셋
  pblntf_ty?: string, pblntf_detail_ty?: string,        // 원시 코드 직접 지정
  corp_cls?: "Y" | "K" | "N" | "E",
  last_report_only?: boolean,   // ⚠️ 기본 false (= last_reprt_at=N). §6.3 참조
  page?: number, page_size?: number,   // 최대 100
  mode?: "page" | "batch",      // batch = 적응형 분할 전수 수집
  continuation_token?: string   // batch 재개용 (60초 한계 대응, §6.3)
}
```

- **90일 제약 자동 우회**: DART 전체시장 검색(corp_code 없음)은 **3개월 초과 기간을 status 100으로 거부**한다(2026-07-31 실측 — 측정 호출도 동일하게 걸림). 처음부터 90일 창으로 나눠 측정·수집한다.
  단 **균등 분할은 쓰지 않는다.** 신고 건수는 2월·8월(결산·주총)에 집중되므로 균등 90일은 시즌에 구조적으로 실패한다(참고 MCP 실측 — `docs/absorbed-from-dart-mcp.md` §1-2). **신고 수 측정 기반 적응형 분할 + 측정 호출 예산 가드**를 쓴다.
- **공정위 프리셋이 1급 시민**: `preset: "ftc_all"` 하나로 J001~J009 전체.
- **`max_pages` 상한은 100.** J 카테고리는 90일 청크당 ~30페이지가 필요하다(2026-07 실측 2주 470건). 상한이 낮으면 **조용히 잘린다** — 참고 MCP가 이 한 줄 때문에 recall 사고를 겪었다.
- **dedup 키는 `rcept_no` 단일.** 같은 회사·같은 날 동일 보고서명의 정당한 다중 제출이 존재하므로 복합키로 "강화"하면 누락이 생긴다(실증).
- **`last_reprt_at=N` 고정** (`last_report_only` 기본 false). `Y`는 정정으로 대체된 **원본 제출분을 지운다** — 실측상 `Y ⊂ N`이고 J 기준 2.1%가 사라진다. 원본 접수일이 없으면 **지연 판정이 불가능**하다. §6.3·`docs/absorbed-from-dart-mcp.md` §3-4c.
- 응답에 `diagnostics` 동봉 — `preset_resolved`(실제 조회 코드), `partial_results`, `dedup_dropped`, `calls_consumed`, `date_chunks`, **`truncated` + 실제 `total_page`**(절단을 조용히 넘기지 않는다).

---

### 5.6 `resolve_entity` — 통합 식별자 해석

```ts
{ query: string, type?: "company" | "group" | "auto", limit?: number }
```

"삼성전자" / "005930" / "00126380" / "삼성"(집단) / 법인등록번호 → `corp_code`, `stock_code`, `기업집단코드`, `소속집단`을 한 번에 해석.

**구현**: DART 고유번호 전체를 SQLite FTS로 로컬 인덱싱 + 기업집단포털 소속회사 목록을 **법인등록번호 기준으로 조인**. 이 조인 테이블이 §5.2 감사 도구의 전제 조건이며, **본 프로젝트 최대의 기술 자산**이다.

**✅ M0 실측 (2026-07-29) — `corpCode.xml` 전량 확보**

| 항목 | 실측값 |
|---|---|
| 총 법인 수 | **118,543개** |
| 상장사(`stock_code` 보유) | **3,980개** (3.4%) |
| ZIP / XML 크기 | 3.6MB / 30MB |
| 제공 필드 | `corp_code`, `corp_name`, `corp_eng_name`, `stock_code`, `modify_date` |
| **`jurir_no` 포함 여부** | **❌ 없음** — 기업개황 API 개별 호출로만 취득 가능 |

> **비상장이 96.6%다.** 공정위공시 대상(`corp_cls="E"`)이 대부분 여기 속하며, 상장사 위주로 설계된 기존 DART MCP들이 사실상 다루지 않는 영역이다.

**✅ R1 매핑 PoC 실측 (2026-07-29, 삼성그룹 67개사)**

이름 정규화(`(주)`·`㈜`·`주식회사`·공백 제거) → `corpCode.xml` 매칭 → `company.json`의 `jurir_no` 대조 방식으로 측정했다.

| 결과 | 건수 | 비율 |
|---|---|---|
| **법인등록번호 일치** | **58** | **86.6%** |
| 번호 불일치(동명 법인) | 3 | 4.5% |
| 이름 미발견 | 6 | 9.0% |

**실패 원인 2가지 — 둘 다 규명됨**

**① 동명 법인 (3건)** — 이건 알고리즘이 *제대로* 작동한 증거다.

```
삼성물산 → corpCode에 2건 존재 (00126229 / 00149655)
           포털 jurirno=1101110015762  vs  DART jurir_no=1101110002975  → 합병 전후 별개 법인
스테코 2건 · 에스티엠 4건 — 상호 동일, 법인 상이
```
> PoC는 첫 후보만 검사(`head -1`)했다. **후보 전체를 순회하며 `jurir_no`로 확정**하면 이 3건은 회수된다. `jurir_no` 검증이 오매칭을 정확히 차단했다는 점이 더 중요하다.

**② 표기 체계 상이 (6건)** — 이름 매칭의 **원리적 한계**

```
포털(한글 음차)                        DART(영문 약어)
삼성에스디아이(주)                  →  삼성SDI
삼성이앤에이(주)                    →  삼성E&A     (XML에서 &amp; 이스케이프)
삼성에프엔위탁관리부동산투자회사(주)  →  삼성FN리츠
```
> 포털은 **한글 음차**, DART는 **영문 약어**를 쓴다. 정규화·유사도로는 풀 수 없다. 음차↔약어 변환 사전을 만드는 건 유지보수 지옥이다.

**→ 채택 전략: 공시 역수집 방식 (이름 매칭 우회)**

이름으로 corp_code를 찾는 대신, **공시에서 corp_code를 수집해 법인등록번호로 조인**한다.

1. `search_disclosures`로 J001·J004·J005·J008·J009 **1년치 전수 수집** → 고유 `corp_code` 집합 확보 (공시의무를 실제로 이행한 회사들)
2. 각 `corp_code` → `company.json` 호출 → **`jurir_no` 확보**
3. 포털 `appnGroupAffiList`의 `jurirno`와 **법인등록번호로 직접 조인** — 이름 불필요
4. 결과를 **매핑 테이블로 영구 캐시**, 월 1회 증분 갱신

> **이 방식의 이점**
> - 표기 체계 차이·동명 법인 문제가 **원천적으로 발생하지 않는다** (조인 키가 법인등록번호)
> - 호출량이 작다 — 실제 J공시 제출 법인은 수천 건 규모로, 118,543건 전수(6일) 대비 하루면 끝난다
> - `bizrno`(사업자등록번호)도 포털·DART 양쪽에 있어 **2차 검증 키**로 쓸 수 있다
>
> **이름 매칭은 fallback으로만 유지한다.** 공시 이력이 없는 회사(= 감사 도구의 "누락 후보")는 공시 역수집으로 잡히지 않으므로, 이때만 이름 매칭 + 사용자 확인 UX를 쓴다.

> 💡 **부수 효과 — 사전 구축 매핑 테이블이 곧 해자다.** 이 매핑 테이블을 패키지에 동봉해 배포하면 사용자는 첫 실행부터 즉시 사용할 수 있다. 경쟁자가 복제하려면 동일한 수집 과정을 반복해야 한다.

---

### 5.7 `get_company_profile` — 기업 종합 프로필

기업개황(2019002) + 최대주주(2019007) + 최대주주 변동(2019008) + 소액주주(2019009) + 임원(2019010) + 직원(2019011) + 타법인출자(2019015) + 기업집단포털 소속회사 개요/임원/주주 를 **병렬 호출 후 합성**.

```ts
{ company: string, sections?: ("basic"|"shareholders"|"executives"|"employees"|"investments"|"group")[] }
```

---

### 5.8 `get_financials` — 재무정보 (DART 기본 풀커버)

```ts
{
  company: string | string[],   // 다중 시 2019017/2022002 자동 선택
  year: string, report: "1Q"|"HY"|"3Q"|"FY",
  statement?: "summary" | "full" | "indicators",  // 2019016 / 2019020 / 2022001
  fs_div?: "CFS" | "OFS",       // 연결/별도
  sj_div?: ("BS"|"IS"|"CIS"|"CF"|"SCE")[]
}
```

**공정위 특화 연결점**: `statement:"summary"` 결과에서 **자본총계·자본금을 추출해 `check_disclosure_duty`의 기준금액 계산에 자동 주입**한다. 재무 조회가 단순 조회로 끝나지 않고 컴플라이언스 판정의 입력이 되는 것이 우리 설계의 특징이다.

---

### 5.9 `get_periodic_report` — 정기보고서 주요정보 (DS002 30종)

```ts
{
  company: string, year: string, report: "1Q"|"HY"|"3Q"|"FY",
  section: "shares_total" | "treasury_stock" | "dividend" | "capital_change"
         | "debt_securities" | "cp_balance" | "short_bond_balance" | "corp_bond_balance"
         | "hybrid_balance" | "conditional_capital_balance"
         | "public_fund_usage" | "private_fund_usage"
         | "auditor_opinion" | "audit_service" | "non_audit_service"
         | "outside_director" | "largest_shareholder" | "largest_shareholder_change"
         | "minority_shareholder" | "executives" | "employees"
         | "unregistered_exec_comp" | "director_comp_approved" | "director_comp_paid"
         | "director_comp_by_type" | "individual_comp_5e" | "top5_comp_5e"
         | "other_corp_investment",
  version?: "v1" | "v2"         // 보수 관련 2026001/2026002 신규 버전 대응
}
```

30개 API를 **1개 도구 + section enum**으로. 다중 section 요청 시 병렬 호출 후 합성.

---

### 5.10 `get_corporate_event` — 주요사항보고서·증권신고서 (DS005 36 + DS006 6)

```ts
{
  company: string,
  event: "bankruptcy" | "business_suspension" | "rehabilitation" | "dissolution"
       | "rights_offering" | "bonus_issue" | "mixed_offering" | "capital_reduction"
       | "creditor_management_start" | "creditor_management_stop" | "litigation"
       | "overseas_listing" | "overseas_delisting" | "cb" | "bw" | "eb"
       | "conditional_capital" | "asset_transfer" | "tangible_asset_acquire"
       | "tangible_asset_dispose" | "other_corp_stock_acquire" | "other_corp_stock_dispose"
       | "business_acquire" | "business_transfer" | "treasury_acquire" | "treasury_dispose"
       | "treasury_trust_contract" | "treasury_trust_cancel"
       | "merger" | "split" | "split_merger" | "stock_exchange"
       | "bond_acquire" | "bond_dispose"
       | "reg_equity" | "reg_debt" | "reg_dr" | "reg_merger" | "reg_split" | "reg_exchange",
  date_from: string, date_to: string,
  timeline?: boolean            // 이벤트 시계열 정렬 반환
}
```

**공정위 연결점**: `other_corp_stock_acquire`, `asset_transfer`, `business_acquire` 등은 **대규모내부거래 공시 대상과 겹친다**. `timeline:true`로 조회하면 "주요사항보고서는 냈는데 대규모내부거래 공시는 안 낸" 케이스를 교차 검증할 수 있다. → §5.2 감사 도구의 보조 신호원.

---

### 5.11 `read_disclosure` — 공시 원문·첨부 파싱

> **P4 해결.** J유형 공시는 전용 API가 없으므로 **원문 파싱이 유일한 구조화 경로**다. 이 도구가 없으면 킬러 기능 대부분이 성립하지 않는다.

```ts
{
  rcept_no: string,             // 접수번호 14자리
  format?: "markdown" | "text" | "raw" | "tables",
  section?: string | string[],  // ★ 대용량 대응: 항목번호/제목으로 부분 조회
                                //   예: "7" | "계열회사와 특수관계인간 거래현황"
  include_attachments?: boolean,
  extract?: "board_date" | "counterparty" | "amount" | "total_balance"
          | "total_assets" | "transaction_type" | "amendment_info" | "all",
  max_bytes?: number,           // 응답 상한 (기본 200KB 상당)
  page_range?: string
}
```

- 공시서류원본파일(2019003) → ZIP → XML 파싱 → 마크다운
- 첨부 HWP/HWPX/PDF/DOCX → 마크다운 (`kordoc` 등 활용)
- **`extract` 모드**: J001 공시 원문에서 이사회 의결일·거래상대방·거래금액·거래유형을 정규식/구조 기반 추출 → `audit_group_disclosures`의 지연 판정 입력으로 사용
- `format: "tables"`: 표만 추출해 CSV/마크다운 표로 (기업집단현황공시는 표 덩어리라 이 모드가 실사용 핵심)

---

### 5.12 `get_disclosure_calendar` — 공시 캘린더 ★신규

> 담당자가 가장 먼저 원하는 것은 조회가 아니라 **"내가 올해 뭘 언제까지 해야 하는가"**다. 특히 신규 지정 집단(2026년 11개 신규: 라인·토스·한국콜마·웅진·오리온 등)에게는 이게 1순위다.

```ts
{
  target: string,                    // 회사명 | 기업집단명
  target_type?: "company" | "group",
  year?: string,                     // 기본: 당해년도
  from?: string, to?: string,
  scope?: ("ftc" | "dart_periodic" | "all")[],
  view?: "calendar" | "upcoming" | "overdue",
  days_ahead?: number                // upcoming 기본 30일
}
```

**포함 의무**

| 의무 | 주기 | 비고 |
|---|---|---|
| 기업집단현황공시 | 분기 + 연1회 | 개별회사/대표회사/동일인용 **3종 구분** (실측 확인) |
| 약관 금융거래 분기공시 | 분기 | 금융계열사 보유 집단만 |
| 하도급대금 결제조건 공시 | 반기 | 대상 여부 판정 포함 |
| 정기보고서 | 사업/반기/분기 | 사업보고서 90일, 반기·분기 45일 |
| 개별 이벤트 기한 | 수시 | `check_disclosure_duty` 판정 결과 연동 |

**반환**: 의무별 `{의무명, 근거조문, 기준일, 기한, D-day, 상태(예정/임박/경과)}` + 월별 캘린더 뷰 + 임박 경고.

---

### 5.13 `draft_disclosure` — 공시 초안·기재요령 ★신규 (킬러 후보)

> **J004 정정률 91%가 이 도구의 존재 이유다.** 담당자는 공시를 못 내는 게 아니라, 한 번에 제대로 쓰지 못해 정정한다.

```ts
{
  form: string,                      // 서식명 (J001 44종 / J005 30종 taxonomy 기반)
  company: string,
  data?: Record<string, any>,        // 이미 아는 값
  reference_count?: number,          // 참고할 타사 사례 수 (기본 3)
  output?: "checklist" | "draft" | "both"
}
```

**동작**
1. 서식별 **기재항목 체크리스트** 제시 (필수/선택 구분)
2. 동일 서식의 **타사 최근 공시를 자동 수집**해 기재 패턴·표현 예시 제공 (`search_disclosures` + `read_disclosure` 연계)
3. 입력값으로 **초안 생성**
4. **누락 항목 경고** — 특히 총거래잔액, 이사회 의결일 등 빠뜨리기 쉬운 항목

> ⚠️ **설계 원칙: 자동 제출 기능은 만들지 않는다.** 초안 생성까지만 한다. 오제출 리스크가 도구의 편익을 압도한다. 최종 검토·제출은 담당자가 DART 접수시스템에서 수행한다.

---

### 5.14 `benchmark_peers` — 피어 벤치마킹 ★신규 (IR)

> IR 담당자가 반나절씩 쓰는 비교표 작성을 자동화한다.

```ts
{
  base: string,
  peers?: string[],                  // 미지정 시 업종코드(induty_code) 기반 자동 선정
  dimensions: ("financials" | "governance" | "compensation"
             | "dividend" | "shareholders" | "disclosure_pattern")[],
  year?: string,
  output?: "table" | "csv"
}
```

| dimension | 비교 내용 | 소스 |
|---|---|---|
| `financials` | 매출·영업이익·ROE·부채비율 | DS003 |
| `governance` | 이사회 구성, 사외이사 비율·변동 | 2020012, 2019010 |
| `compensation` | 임원 보수(5억 이상), 총보수 | 2019012, 2019013 |
| `dividend` | 배당성향·배당이력 | 2019005 |
| `shareholders` | 최대주주·소액주주 구성, 지분변동 | 2019007~9, DS004 |
| `disclosure_pattern` | 공시 빈도·유형 분포·정정률 | DS001 |

---

### 5.15 기존 도구 확장 (실무 요구 반영)

> 새 도구를 늘리는 대신 옵션으로 흡수해 컨텍스트 예산을 지킨다.

| 도구 | 추가 옵션 | 실무 가치 |
|---|---|---|
| `check_disclosure_duty` | `estimate_penalty` — 지연일수 기준 **예상 과태료 산정** (「과태료 부과기준」 고시 기반) | **"늦으면 얼마 뭅니까?"** — 경영진 보고·우선순위 판단의 근거. 실무 임팩트 최상위 |
| `check_disclosure_duty` | `form_suggestion` — 44종 중 해당 서식 추천 | 서식 오선택 방지 |
| `audit_group_disclosures` | `amendment_analysis` — **정정 유발 항목 분석** | 어느 항목에서 정정이 반복되는지 → 사전 예방 |
| `track_internal_transactions` | `running_balance` — 동일 상대방·동일 유형 **총거래잔액 추적** | 공시 기재 필수항목인데 대부분 수기 관리 중 |
| `track_internal_transactions` | `split_detection` — 기준금액 직하 반복 패턴 탐지 | 회피로 오인될 패턴 자가진단 |
| `track_internal_transactions` | `omnibus_financial` — 약관 금융거래 분기공시 전용 트랙 | 금융계열사 보유 집단 필수 |
| `search_disclosures` | `since` — 마지막 확인 시점 이후 신규 공시만 | 자사·경쟁사 모니터링 |
| `get_periodic_report` | `compare` — 동일 섹션 다중사 비교 | 벤치마킹 |
| `get_group_structure` | `diff` — 두 시점 간 계열사 변동 비교 | 편입·제외 파악 |
| **전 도구 공통** | `output_format: "json" \| "markdown" \| "csv"` | **실무자는 결국 엑셀로 작업한다.** CSV 출력은 선택이 아니라 필수 |

> ⚠️ **과태료 추정 면책**: 과태료는 공정위 재량과 감경 사유(자진시정·초범·경미성 등)에 따라 달라진다. 반드시 **"고시 기준 단순 산정값이며 실제 부과액과 다를 수 있음"**을 결과에 병기하고, 감경 요건도 함께 제시한다.

---

### 5.16 도구 요약표

| # | 도구 | 분류 | 소스 | 주 사용자 | 페인포인트 |
|---|---|---|---|---|---|
| 1 | `check_disclosure_duty` | ★킬러 | 룰엔진 + DART + 포털 | 공시담당 | P1, P2, P10 |
| 2 | `audit_group_disclosures` | ★킬러 | DART + 포털 결합 | 공정거래팀 | P3 |
| 3 | `get_group_structure` | ★킬러 | 포털 15 API | 공통 | P5 |
| 4 | `track_internal_transactions` | ★킬러 | J001/J008 | 공시담당 | P6 |
| 5 | `search_disclosures` | 기반 | DS001 | 공통 | 전반 |
| 6 | `resolve_entity` | 기반 | DS001 + 포털 | 공통 | 전반 |
| 7 | `get_company_profile` | 기본 | DS001+DS002+포털 | IR | - |
| 8 | `get_financials` | 기본 | DS003 | IR | - |
| 9 | `get_periodic_report` | 기본 | DS002 (30) | IR | - |
| 10 | `get_corporate_event` | 기본 | DS005(36)+DS006(6) | IR | - |
| 11 | `read_disclosure` | 기반 | DS001 + 파서 | 공통 | P4 |
| 12 | `get_disclosure_calendar` | ★확장 | 룰엔진 + 포털 | 공시담당 | P7 |
| 13 | `draft_disclosure` | ★확장 | 서식 taxonomy + 원문 | 공시담당 | P8 |
| 14 | `benchmark_peers` | ★확장 | DS002+DS003+DS004 | IR | P9 |

**추가 페인포인트** (§1.2 확장)

| # | 상황 | 대응 도구 |
|---|---|---|
| P7 | "올해 우리가 뭘 언제까지 해야 하지?" (특히 신규 지정 집단) | `get_disclosure_calendar` |
| P8 | "어떻게 써야 정정이 안 나지?" (J004 정정률 91%) | `draft_disclosure` |
| P9 | "피어 비교표를 만들어야 하는데 반나절 걸림" | `benchmark_peers` |
| P10 | "이거 늦으면 과태료 얼마인지 임원 보고해야 함" | `check_disclosure_duty(estimate_penalty)` |

**v1.1 추가 검토**: `discover_tools` / `execute_tool` 메타 패턴 — 도구가 14개가 되었으므로 XBRL·지분공시 상세 등 저빈도 도구를 숨겨 컨텍스트를 절약한다. korean-law-mcp가 19개→10개로 통폐합해 "컨텍스트 52% 감축"을 달성한 경로를 따른다.

---

## 6. 아키텍처

### 6.1 기술 스택

| 레이어 | 선택 | 근거 |
|---|---|---|
| 언어 | TypeScript 5.x | 벤치마크 2종과 동일, 타입 안전한 스키마 |
| 런타임 | **Node.js ≥ 22.5** | `node:sqlite` 내장 요건. Node 20은 2026-04 EOL이라 실익 없음 |
| MCP | `@modelcontextprotocol/sdk` (최신) | 표준 |
| 스키마 | `zod` v4 고정 | MCP 도구 스키마 (버전 혼용 이슈 주의) |
| HTTP | `undici` / native fetch | 의존성 최소화. **connect/read 타임아웃 분리**(10s / 100s) |
| 캐시 | **`node:sqlite`** (내장) | ✅ 결정(2026-07-29). corp_code 인덱스 + 원문 캐시. **런타임 의존성 0 유지**. Node 24.13/SQLite 3.50.4에서 **FTS5 `tokenize='trigram'` 한글 부분일치 실측 확인**(참고 MCP 요구 3.34+ 충족). `better-sqlite3`는 네이티브 빌드라 `npx setup` 설치 실패 1순위 원인이므로 채택 안 함. ⚠️ API가 아직 experimental → **얇은 어댑터로 감싸 교체 가능하게** 두고, 기동 시 `ExperimentalWarning`은 stderr로만 나가게 처리 |
| 문서 파싱 | `kordoc` (HWP/HWPX/PDF/DOCX) | 검증된 한국어 문서 엔진 |
| XML/ZIP | `fast-xml-parser`, `yauzl` | DART 원본 처리 |
| 테스트 | `vitest` | - |
| 배포 | npm + Claude Code plugin marketplace | §8 |

### 6.2 디렉터리 구조

```
gongsi-mcp/
├── src/
│   ├── index.ts                 # MCP 서버 엔트리 (stdio)
│   ├── setup.ts                 # npx ... setup 대화형 마법사
│   ├── tools/                   # 11개 도구 핸들러
│   │   ├── check-disclosure-duty.ts
│   │   ├── audit-group-disclosures.ts
│   │   ├── get-group-structure.ts
│   │   ├── track-internal-transactions.ts
│   │   └── ...
│   ├── clients/
│   │   ├── dart.ts              # OpenDART 클라이언트 (rate limit, retry)
│   │   └── egroup.ts            # 기업집단포털 클라이언트
│   ├── rules/                   # ★ 룰 엔진 (핵심 자산)
│   │   ├── thresholds.ts        # 기준금액 계산
│   │   ├── deadlines.ts         # 기한 계산 + 영업일 캘린더
│   │   ├── duties/              # 의무 유형별 판정 로직
│   │   └── legal-refs.ts        # 조문 인용 데이터
│   ├── parsers/
│   │   ├── document.ts          # 공시 원문 XML → 마크다운
│   │   ├── attachment.ts        # HWP/PDF → 마크다운
│   │   └── extractors/          # J001 등 유형별 필드 추출
│   ├── resolver/
│   │   ├── corp-index.ts        # corp_code SQLite FTS
│   │   └── group-mapping.ts     # ★ 법인등록번호 ↔ corp_code 매핑
│   ├── lib/ (cache, ratelimit, errors, format)
│   └── types/
├── data/
│   ├── holidays.json            # 한국 공휴일 (영업일 계산)
│   ├── ftc-codes.json           # J001~J009 코드표
│   └── legal-refs.json          # 조문 원문 캐시
├── test/
├── docs/ (API.md, ARCHITECTURE.md, COMPLIANCE.md, DEVELOPMENT.md)
├── .claude-plugin/              # Claude Code 플러그인 마켓플레이스용
└── README.md
```

### 6.3 횡단 관심사

| 항목 | 설계 |
|---|---|
| **API 키** | `DART_API_KEY`, `EGROUP_API_KEY`(공공데이터포털) 환경변수. 하드코딩 금지, `.env` 지원. **예외 메시지·로그에 키가 절대 나타나지 않도록** 재포장 + 회귀 테스트로 고정 |
| **레이트리밋** | DART 일 20,000 / 포털 개발계정 일 10,000. **KST 일별 카운터 영속화**(DART 한도 리셋이 KST 자정 기준). 경고 임계 도달 시 로그, 하드스톱 시 **원문 다운로드만 거부하고 목록 조회는 허용**. 초과 시 한글 에러 + `reset_at_kst` |
| **캐시** | 계층별 TTL — corp_code 7일, **기업집단 구조 1년**(포털이 연 1회 5/1 갱신, §4.2 실측), 공시원문 영구, 재무 24시간. **매년 5월 초 강제 무효화**. ⚠️ **공시 목록(검색 결과)은 캐시하지 않는다** — 공시담당자에게 신선도가 최우선이며, "방금 접수된 공시"가 안 보이면 도구를 신뢰하지 않는다 |
| **에러** | 전 메시지 한글. DART status 코드(010/011/013/020/100/800/900) 별 조치 안내 포함. **에러 응답 규격 통일**(`{error, message, details}`)이며 **도구는 예외를 밖으로 던지지 않는다**(스택트레이스 노출 방지) |
| **응답 크기** | 대용량은 요약 + 페이지네이션. 토큰 폭발 방지를 위해 기본 응답에 상한. **`compact` 모드**(schema 헤더 + 값 배열, 파생 가능 필드 제외) 제공 — 토큰 30~40% 절감 |
| **⚠️ 60초 벽** | **MCP 클라이언트는 약 60초에 호출을 끊는다**(참고 MCP 실측: J 1년 단일 호출 68초, 결산시즌 3개월 66초 timeout). 서버 내부 타임아웃을 늘려도 소용없다. `audit_group_disclosures`는 이 설계 없이는 성립하지 않는다.<br>**M2 = ①+③ / M3 = ②** ✅ 결정(2026-07-29)<br>① **사전 예측 + 분할 안내** — 대상 건수를 먼저 재고(측정 호출 1회), 60초 안에 못 끝낼 범위면 즉시 `range_too_large` + `estimated_seconds` + `suggested_splits[]`를 반환. 도구가 "그냥 죽는" 사고를 가장 싸게 막는다<br>② `continuation_token`으로 이어받기 (M3)<br>③ **per-task 타임아웃 + `Promise.allSettled`로 부분 결과 보존** — 한 계열사가 늦어도 나머지는 살리고 `partial_results=true` |
| **⚠️ `last_reprt_at`** | **기본 `N` 고정.** `Y`(최종보고서만)는 정정으로 대체된 **원본 제출분을 지운다**. 실측(2026-02-23~03-06): 전체 N=14,597 / Y=12,794, J는 N=3,505 / Y=3,430으로 **`Y ⊂ N`이고 Y가 추가로 주는 건 0건**. 사라지는 J 75건의 `rm`은 `공정`(공정위+정정) = 원본들이다. **원본 접수일이 없으면 "의결일 대비 기한 초과" 판정 자체가 불가능**하고, J004 정정률 91%라 정정 추적도 막힌다. 참고 MCP는 `Y` 하드코딩(투자 관점) — **우리는 정반대** |
| **진단** | 모든 수집형 응답에 `diagnostics` 동봉 — `partial_results` / `calls_consumed` / `dedup_dropped` / `preset_resolved` / `date_chunks` / **`truncated`+실제 `total_page`**. **불완전한 결과를 완전한 것처럼 보고하는 것이 이 제품 최대의 리스크**(§11). 감사 도구는 `partial_results`를 판정 결과에 **그대로 전파**해야 한다 — 불완전 수집 위에서 "누락 없음"을 결론내면 안 된다 |
| **환경변수** | 접두사 통일 + **기동 시 미인식 env 경고**. 참고 MCP는 이름 불일치로 설정 3개가 조용히 무시되고 있었다 |
| **로그** | **stdout은 MCP 프로토콜 전용.** 모든 로그는 stderr + 회전 파일 |
| **개인정보** | 임원 개인정보·보수 등은 공개 공시 범위 내에서만. 별도 저장 안 함 |
| **감사 로그** | 판정 결과에 사용된 입력·근거를 항상 동봉(재현가능성) |

---

## 7. 사용 시나리오 (Acceptance Scenarios)

> PRD 검수와 README 작성에 그대로 쓰는 시나리오. 각각이 통합테스트 케이스가 된다.

**S1 — 공시 대상 판정**
> "우리 회사가 계열사에 80억 대여하려는데 공시 대상이야?"
→ `resolve_entity` → `get_financials`(자본총계·자본금) → `check_disclosure_duty`
→ **"공시 대상입니다. 기준 60억(= min(100억, 자본총계 1,200억×5%)). 이사회 사전 의결 필수. 상장사이므로 의결 후 3영업일 이내(2026-08-05, 영업일 조정 반영) 공시. 지연 시 과태료 500만원 + 1일당 10만원. 근거: 공정거래법 §26①, 시행령 §33①, 고시 §6①"**

**S2 — 기한 역산**
> "내일 이사회에 올리면 언제까지 공시해야 해?"

**S3 — 셀프 점검**
> "우리 집단 작년 공시 중 지연되거나 빠진 거 찾아줘"
→ `audit_group_disclosures` → 계열사 전수 대사 → 지연 후보 N건 + 근거 링크

**S4 — 타사 벤치마킹**
> "다른 회사들은 기업집단현황공시 임원현황을 어떻게 썼어?"
→ `search_disclosures(preset:"group_status")` → `read_disclosure(format:"tables")`

**S5 — 구조 파악**
> "OO그룹 순환출자 구조 그려줘"
→ `get_group_structure(view:"circular", format:"mermaid")`

**S6 — 내부거래 집계**
> "작년 우리 집단 대규모내부거래 상대방별로 집계해줘"
→ `track_internal_transactions(group_by:"counterparty")`

**S7 — 회피 패턴 점검**
> "기준금액 바로 아래로 쪼갠 거래 없는지 봐줘"

**S8 — 기본 재무 (필수 기본기)**
> "삼성전자 최근 3년 연결 재무제표 보여줘" → `get_financials`

**S9 — 신규 지정 대응**
> "우리가 올해 공시대상기업집단에 새로 지정됐는데 뭘 해야 해?"
→ `get_group_structure(view:"changes")` + `check_disclosure_duty(duty:"auto")` → 의무 목록·기한 캘린더

**S10 — 공익법인**
> "재단이 계열사 주식을 취득하는데 이사회 의결 필요해?" → §29 판정

**S11 — 연간 일정 파악** (확장)
> "우리 집단 올해 남은 공시 일정 정리해줘"
→ `get_disclosure_calendar(view:"upcoming")` → 의무별 기한·D-day·근거조문

**S12 — 작성 지원** (확장)
> "특수관계인에대한자금대여 공시 써야 하는데 뭘 적어야 해?"
→ `draft_disclosure` → 기재항목 체크리스트 + 타사 최근 3건 기재 패턴 + 초안

**S13 — 과태료 예측** (확장)
> "3일 늦게 공시했는데 과태료 얼마나 나올까?"
→ `check_disclosure_duty(estimate_penalty:true)` → 고시 기준 산정액 + 감경 요건 + 면책 고지

**S14 — IR 비교표** (확장)
> "동종업계 5개사랑 임원보수·배당 비교표 CSV로 만들어줘"
→ `benchmark_peers(dimensions:["compensation","dividend"], output:"csv")`

**S15 — 잔액 관리** (확장)
> "A사와의 자금대여 총거래잔액 지금 얼마야?"
→ `track_internal_transactions(running_balance:true)` — 공시 기재 필수항목

---

## 8. 배포·설치

### 8.1 3단 온보딩

**① npx 원클릭 (권장)**
```bash
npx -y gongsi-mcp setup
```
대화형 마법사: API 키 2종 입력 → 클라이언트 자동 감지(Claude Desktop/Code, Cursor, Windsurf, VS Code, Zed, Gemini CLI) → 설정 파일 자동 수정. **Windows는 `cmd /c npx` 래핑 자동 처리** (한국 기업 실무자 대부분 Windows).

**② Claude Code 플러그인 마켓플레이스**
```
/plugin marketplace add <org>/gongsi-mcp
/plugin install dart-ftc@dart-ftc-marketplace
```
설치 중 API 키 입력 프롬프트. korean-law-mcp가 확산에 쓴 경로.

**③ 수동 설정** — JSON 스니펫 제공

### 8.2 API 키 발급 안내 (README 최상단, 실무자는 여기서 이탈함)

| 키 | 발급처 | 소요 | 한도 |
|---|---|---|---|
| `DART_API_KEY` | opendart.fss.or.kr | 즉시 | 일 20,000 |
| `EGROUP_API_KEY` | data.go.kr (자동승인) | 즉시 | 일 10,000 |

> **UX 결정**: 기업집단포털 키가 없어도 DART 단독 기능은 전부 동작하게 만든다(graceful degradation). 킬러 기능 사용 시에만 키를 요구하고 발급 링크를 안내한다. **2개 키 요구가 진입장벽이 되지 않도록** 하는 것이 확산의 관건.

### 8.3 README 전략 (korean-law-mcp 벤치마크)

1. 최상단: 배지(npm·MCP 버전·MIT) + **한 줄 가치제안** — "공시 과태료, 맞기 전에 잡습니다"
2. **문제 제기**: 2025년 위반 146건·과태료 8.9억·원인은 "담당자 업무 미숙" (공정위 인용)
3. **30초 설치** 블록
4. **Before/After**: "시행령 찾아 계산 30분" → "질문 한 줄 5초"
5. 실무 시나리오 10종 (§7) — 담당자가 자기 상황을 즉시 발견하게
6. 도구 11종 표
7. **"왜 기존 DART MCP로는 안 되는가"** 비교표 (§2.1)
8. 면책·데이터 출처 명시
9. 데모 GIF/영상

---

## 9. 성공 지표

| 구분 | 지표 | 3개월 | 12개월 |
|---|---|---|---|
| 확산 | GitHub ★ | 100 | 500+ |
| 확산 | npm 주간 다운로드 | 200 | 1,000 |
| 확산 | awesome-mcp-korea 등재 | ✅ | - |
| 품질 | 공시의무 판정 정확도 (공정위 매뉴얼 케이스 대조) | 95% | 99% |
| 품질 | 감사 도구 오탐률(false positive) | < 20% | < 10% |
| 품질 | J001~J009 원문 파싱 성공률 | 90% | 98% |
| 사용 | 킬러 도구(1~4) 호출 비중 | 30% | 50% |
| 신뢰 | 실무 도입 사례 공개 | 1건 | 5건 |

---

## 10. 로드맵

### M0 — 기술 검증 (1주) ⚠️ 최우선

**이 단계가 실패하면 제품 컨셉을 수정해야 한다. 반드시 먼저 한다.**

**진행 상황 (2026-07-28 기준)**

- [x] DART `pblntf_detail_ty=J001~J009` 실호출 → **완료.** 유효코드 5개(J001·J004·J005·J008·J009) 확정, 나머지 4개 비활성
- [x] J001 서식 taxonomy 44종 / J005 30종 / J004 3종 **실측 확보** → 룰 엔진 enum 근거 확정
- [x] 기업집단포털 엔드포인트·기관코드(`1130000`)·갱신주기 확인 → **완료.** 연 1회(5/1 기준) 스냅샷임을 확인
- [x] DART 기업개황 `jurir_no` 제공 확인 → **완료. R1 원리적 해소** (법인등록번호 조인 가능)
- [x] 기업집단포털 4개 API 실호출 → **완료.** 엔드포인트 4종 확정, `User-Agent` 필수 함정 발견
- [x] 법인등록번호 ↔ corp_code **매핑률 측정** → **완료. 86.6%**(이름매칭 기준, 삼성 67개사). 실패 9건 원인 규명 → **공시 역수집 방식**으로 전환 결정
- [ ] 공시 역수집 매핑 테이블 구축 (J공시 1년치 → corp_code → jurir_no → 포털 조인)
- [x] J유형 공시 원문 구조 분석 → **완료. R2 해소** (이사회 의결일·총거래잔액·자산총액 모두 원문에 존재, UTF-8 정형 XML, `ACODE`로 서식 식별)
- [ ] J001 44종 **서식별 필드 매핑 테이블 작성** (신규 과제 — M2로 이관)
- [ ] 서식 버전(`FORMULA-VERSION`) 분기 정책 수립 (6.0 / 6.2 혼재 확인)
- [x] 고시 원문으로 공시 기한·기준일 **확정** → **완료.** 고시 2종 전문 확보. **"상장 1일" 오류 정정 → 3영업일**
- [x] 「과태료 부과기준」 고시 + **시행령 별표9** 수집 → **완료.** 기본금액표·가중감경·면제·상한 전부 확보 (부록 B)
- [ ] 거래금액별 비율표(대규모내부거래 고시 Ⅵ.2, 이미지) 원문 확보
- [x] 하도급법 §13-3 결제조건 공시 기한 확정 → **완료.** 시행령 §8의2② 원문: **매 반기 종료일 다음 날부터 45일 이내** (달력일, 상반기 ~8/14·하반기 ~익년 2/14). `subcontractPaymentDeadline()` 구현·테스트 완료

### 우선순위 판단 매트릭스

M0에서 확보한 사실을 반영해 14개 도구를 4축으로 평가했다.

| 도구 | 실무가치 | 구현비용 | 리스크 | 차별화 | 의존성 | 순위 |
|---|---|---|---|---|---|---|
| `check_disclosure_duty` | **최상** | 중 | 낮음 | **최상** | **없음** | **1** |
| `get_group_structure` | 상 | **낮음** | 낮음 | **최상** | 포털API(검증완료) | **2** |
| `get_disclosure_calendar` | 상 | 낮음 | 낮음 | 상 | 룰엔진 | **3** |
| `search_disclosures` | 중(수단) | 낮음 | 낮음 | 중(J프리셋) | 없음 | 4 |
| `read_disclosure` | 중(수단) | 중 | 낮음 | 중 | 없음 | 5 |
| `resolve_entity` | 중(수단) | 중 | 중 | 중 | corpCode | 6 |
| `track_internal_transactions` | 상 | 중 | 중 | 상 | read_disclosure | 7 |
| `get_financials` | 중 | 낮음 | 낮음 | **없음** | 없음 | 8 |
| `get_periodic_report` | 중 | 중 | 낮음 | **없음** | 없음 | 9 |
| `get_corporate_event` | 중 | 중 | 낮음 | **없음** | 없음 | 10 |
| `get_company_profile` | 낮음 | 낮음 | 낮음 | 없음 | 여러 API | 11 |
| `audit_group_disclosures` | **최상** | **최상** | **최상(오탐)** | **최상** | 전부 | 12 |
| `benchmark_peers` | 중 | 상 | 중 | 중 | 여러 도구 | 13 |
| `draft_disclosure` | 상 | **최상** | 상 | **최상** | read+서식 | 14 |

**판단에서 나온 3가지 결론**

1. **`check_disclosure_duty`는 의존성이 0이다.** 룰 엔진은 순수 계산 함수이고, 자본총계·자본금만 있으면 API 없이도 판정된다. 가장 먼저 만들 수 있고 가장 큰 차별화를 낸다. **여기서 시작한다.**
2. **`get_group_structure`가 가성비 1위다.** 포털 API 4종은 이미 실호출을 마쳤고, 붙이기만 하면 된다. 구현비용은 최저인데 차별화는 최상이다 — 경쟁 MCP 중 기업집단포털을 쓰는 곳이 하나도 없다.
3. **`audit_group_disclosures`는 뒤로 미룬다.** 킬러 #2지만 매핑 테이블·시점 보정·원문 파싱이 모두 선행돼야 하고, **오탐이 제품 신뢰를 한 번에 무너뜨린다**. 기반이 검증된 뒤에 연다.

### 최적화 결정 — 큰 도구를 쪼개서 앞당긴다

무거운 도구를 통째로 미루는 대신, **가벼운 부분만 먼저 출시**한다.

| 도구 | v1.0에 넣는 것 | 뒤로 미루는 것 |
|---|---|---|
| `audit_group_disclosures` | **단일 회사 감사** (corp_code만 필요, 매핑 테이블 불요) | 집단 전체 감사 (매핑 테이블 필요) |
| `draft_disclosure` | **서식별 기재항목 체크리스트** (ACODE 매핑만으로 가능) | 타사 사례 수집 + 초안 생성 |
| `read_disclosure` | **트랙 A 8종 라벨 추출** | 트랙 B 4종, 첨부 HWP/PDF 파싱 |

### M1 — 룰 엔진 코어 (1주) · API 의존도 0

`rules/thresholds.ts` `deadlines.ts` `penalties.ts` `business-days.ts` + 법령 데이터 JSON.
**순수 함수 + 단위 테스트만.** MCP 서버도, API 클라이언트도 아직 없다.
검증: 실제 J001 공시 사례로 회귀 테스트 세트 구축 (의결일→공시일 지연 판정, 기준금액, 과태료).

> 이 단계를 독립시키는 이유: 제품의 핵심 자산이면서 외부 의존이 전혀 없어 **가장 빠르고 확실하게 검증**된다. 여기가 틀리면 나머지가 무의미하다.

### M2 — 최소 출시 가능 제품 (1.5주)

스캐폴딩 + MCP 서버 골격 + DART/포털 클라이언트(**User-Agent 필수**) + npx setup 마법사
도구 3종: `check_disclosure_duty` · `get_group_structure` · `resolve_entity`

> **이 시점에 이미 배포 가능하다.** 경쟁 MCP에 없는 기능만으로 구성돼 있어, 도구 3개짜리 v0.1이라도 대체재가 없다.

### M3 — 공시 축 (2주)

`search_disclosures`(J 프리셋) · `read_disclosure`(트랙 A) · `track_internal_transactions` · `get_disclosure_calendar`
`audit`의 **단일 회사 모드** · `draft`의 **체크리스트 모드**

### M4 — DART 기본기 (1.5주)

`get_financials` · `get_periodic_report` · `get_corporate_event` · `get_company_profile`

> 차별화는 없지만 **"DART 기본은 다 된다"가 사용자 요구사항**이다. 경쟁자가 이미 잘하는 영역이라 앞에 두면 출시만 늦어지므로 여기 배치한다. 구현 자체는 단순 래핑이라 빠르다.

### M5 — 공개 (1주)

README(§8.3 전략) · 문서 · 데모 · **npm 배포 + 플러그인 마켓플레이스 + awesome-mcp-korea PR**

### v1.1 — 신뢰가 쌓인 뒤

- **`audit_group_disclosures` 집단 전체 모드** (공시 역수집 매핑 테이블 선행)
- `draft_disclosure` 타사 사례·초안 생성
- `benchmark_peers`
- `read_disclosure` 트랙 B + 첨부 HWP/PDF
- `discover_tools`/`execute_tool` 메타 패턴 (도구 14개 → 컨텍스트 절감)

### v1.2 이후
XBRL 파싱 · 하도급대금 공시 심화 · 원격 MCP 엔드포인트

---

**총 7주. v1.0 스코프는 도구 11종**(14종 중 `benchmark_peers` 제외, `audit`·`draft`는 축소 모드).

---

## 11. 리스크

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| R1 | ~~법인등록번호 ↔ corp_code 매핑 실패~~ | ~~치명~~ | **✅ 원리적 해소 (2026-07-28)**. DART가 `jurir_no`를 제공함을 실측 확인. 잔여 과제는 매핑률 측정뿐 |
| R2 | ~~J유형 공시 원문 파싱 실패~~ | ~~높음~~ | **✅ 해소 (2026-07-29)**. 원문이 UTF-8 정형 XML이고 `ACODE`로 서식 식별 가능. 이사회 의결일·총거래잔액·자산총액이 모두 원문 필드로 존재 |
| R2' | 서식 44종 필드 매핑 + 서식 개정 대응 부담 | 중간 — 공수 | 파싱은 가능하나 **서식별 매핑 테이블 44종**이 필요하고 `FORMULA-VERSION`(6.0/6.2 혼재)별 분기가 필요하다. 빈도 상위 10종(전체의 약 70%)부터 구현하고 미지원 서식은 원문 표 그대로 반환해 degrade |
| R3 | **오판정으로 사용자에게 실질 손해** | 치명 — 법적/신뢰 | ①모든 판정에 근거·계산식 동봉 ②"참고용, 최종 판단은 담당자" 면책 명시 ③불확실 시 `insufficient_data` 반환, 추측 금지 |
| R4 | **법령·고시 개정으로 룰 노후화** | 높음 | 룰을 코드가 아닌 **데이터(JSON)로 분리**, 시행일 기준 버저닝. 개정 모니터링 체크리스트 |
| R5 | 기업집단포털 API 불안정/스키마 변경 | 중간 | 캐시 폴백, 포털 미사용 시 degrade |
| R6 | `korean-dart-mcp`가 공정위 기능 추가 | 중간 | 룰 엔진·감사 로직은 도메인 지식 집약이라 단기 복제 난이도 높음. **선점 + 실무자 커뮤니티 확보로 방어** |
| R7 | API 한도 초과(대량 감사 시) | 중간 | 캐시 적극 활용, 배치 진행률 표시, 한도 잔량 사전 경고 |
| R8 | 2개 API 키 요구로 진입장벽 | 중간 | DART 단독 동작 보장(§8.2), setup 마법사에서 발급 링크 직접 안내. **추가로 data.go.kr은 API별 활용신청이 필요하므로 신청 대상 4건을 README에 명시** |
| R9 | **포털(연1회 5/1 스냅샷) ↔ DART(실시간) 시점 불일치** | 높음 — 감사 오탐 | 연중 신규 편입사를 "누락"으로 오탐할 수 있음. 감사 결과에 **데이터 기준일을 명시**하고, 기준일 이후 편입 회사는 별도 구분 표기. `get_group_structure(view:"changes")`로 보정 |
| R10 | **과태료 추정이 실제 부과액과 상이** | 중간 — 신뢰 손상 | 재량·감경사유 반영 불가. "고시 기준 단순 산정값" 명시 + 감경 요건 병기 + 확정액이 아님을 결과마다 경고 |

---

## 12. 열린 질문 (구현 전 확정)

1. **패키지·저장소 명**: `gongsi-mcp` 확정 (2026-08-05, npm 미선점 확인. 구 코드명 `dart-ftc-mcp`에서 rename)
2. 감사 도구 기본 스코프: 집단 전체(무거움) vs 단일 회사(가벼움) — 기본값 결정 필요
3. 영업일 캘린더 데이터 소스: 내장 JSON vs 외부 API (내장 시 연 1회 갱신 필요)
4. ~~공시 기한 "1일 이내"의 기산점~~ → **해소.** 고시 §6① 원문 확인 결과 **상장 3영업일 / 비상장 7영업일**. 남은 확인사항은 영업일 기산점(의결일 당일 포함 여부)뿐이며, 실제 공시 사례 대조로 확정한다
5. 라이선스: MIT 권장 (벤치마크 2종 모두 MIT)

---

## 부록 A. 핵심 판정 기준 요약 (치트시트)

> ✅ **전 항목 법령·고시 원문 확인 완료 (2026-07-29)**. 초안의 "상장 1일" 기한은 오류였고 **3영업일**로 정정했다.

```
[영업일 정의 — 양 고시 공통]
  영업일 = 공휴일, 토요일, 근로자의 날(5/1)을 제외한 날
  기한 만료일이 영업일 아니면 → 다음 최초 영업일

[대규모내부거래 — 법 §26 / 령 §33 / 고시(이사회의결·공시)]
  기준금액 = min(100억, max(5억, max(자본총계, 자본금) × 5%))
    · 자본총계 = 주주총회 승인된 최근 사업연도말 재무제표상 자본총계
    · 자본금   = 이사회 의결일의 직전일 자본금
  거래유형 = 자금 | 유가증권 | 자산 | 상품·용역(동일인등 출자 계열사 대상)
  금액산정 = 실제 거래금액. 단 담보제공=담보한도액 /
             부동산임대차=연간임대료+보증금 환산액 / 보험=보험료총액 /
             상품·용역=분기 합계액
  판단단위 = 자금·유가증권·자산 → 동일 상대방 + 동일 거래대상
             상품·용역           → 동일 상대방
  절차     = 사전 이사회 의결 → 공시
  기한     = 상장 3영업일 / 비상장·공익법인 7영업일     ← 고시 §6①
  재의결   = 거래목적·대상·상대방 변경, 금액·단가·이자율 20%↑ 증감,
             계약기간 등 중대변경, 자동연장조항에 의한 연장
  제외     = 부수적 거래(새 거래관계 미성립) / 거래조건 결정불가 행위

[약관 금융거래 특례 — 고시 §9]   ← 트랙 B, ACODE 80701·80751·80752·80754
  ①계열 금융회사의 약관에 의한 일상 금융거래 → 이사회 의결 불요
     (단, 사모사채인수 등 특정 조건 부기 거래는 의결 필요)
  ②그 외 약관 금융거래 → 분기별 일괄 의결 가능
     (수익증권은 1년 이내 기간 정해 일괄 의결 가능)
  ③①의 공시기한 = 분기 종료 후 익월 10영업일까지
  ④②의 공시기한 = 상장 3영업일 / 비상장 7영업일
  ⑤단기금융상품(만기·중도환매수수료 없고 수시입출금)
     = 분기 종료 후 익월 10영업일까지 일괄공시 가능

[상품·용역 특례 — 고시 §9의2]     ← ACODE 80703
  ①1년 이내 거래기간 정해 일괄 이사회 의결 가능
  ②실제 거래금액이 의결금액 대비 20%↑ 감소 시
     → 이사회 의결 없이 분기종료 후 45일 이내 실제금액 공시
  ③분기 중 대규모내부거래 해당 예상 시 → 미리 의결·공시
  ④계약체결방식은 계약 건별 의결·공시(확정 불가 시 유형별 일괄)

[비상장사 중요사항 — 법 §27 / 령 §34 / 고시(중요사항공시) §5의2]
  대상회사 = 공시집단 소속 + 비상장 + 금융·보험업 제외
             & (자산총액 100억↑ OR 100억↓ & 특수관계인 지분 20%↑)
             (청산·1년↑ 휴업 제외)
  임계     = 고정자산 10% | 타법인(계열 제외)주식 5% | 증여 1%
             | 담보제공·채무보증 5% | 채무면제·인수 5%
             | 증자·감자·CB·BW 결정 시 | 최대·주요주주 지분 1%↑ 변동
             | 영업양수도·합병·분할·주식교환이전·해산·회생·관리절차
  기준시점 = 사업연도 종료 후 3개월 경과일 ~ 다음 사업연도 종료 후 3개월
             (신설법인은 설립 당시 납입자본금)
  자기자본 = 자기자본 < 자본금이면 → 최근 자본금으로 본다
  기한     = 사유 발생일부터 7영업일                    ← 고시 §5의2④
             (주요주주 지분변동은 분기 공시)
  사유발생일 = 이사회 결의(위원회 포함) 또는 대표이사 등
               사실상 권한자의 결정이 있은 때
  중복배제 = 법 §26로 공시되는 사항 제외 / 자본시장법 공시로 갈음 가능

[기업집단현황 — 법 §28 / 령 §35 / 고시(중요사항공시) §4~5]
  대상     = 공시집단 소속 전 회사
             (자산 100억↓ & 청산·1년↑휴업 회사만 제외)
  작성주체 = 대표회사(취합·공시) / 개별회사 / 동일인   ← J004 서식 3종의 근거
    · 대표회사 전담: 계열제외 내역, 자산 100억↓ 청산·휴업사 정보,
                     순환출자 현황, 개별사 작성곤란 사항
  연1회 항목 = 일반현황 / 임원·이사회 운영 / 주식소유현황 /
               특수관계인 거래현황 대부분 / 순환출자 현황 /
               지주회사등 아닌 계열사 / 동일인의 국외계열사
  분기 항목  = 계열사간 채무보증 / 상장회사 상품·용역거래 내역 /
               순환출자 변동내역 / 금융·보험사 의결권 행사
  기한     = 연1회 매년 5월 31일                        ← 고시 §5②1
             분기별 매 분기 종료 후 2개월               ← 고시 §5②2
             (→ 2/28 · 5/31 · 8/31 · 11/30)
  상품·용역 거래내역 작성기준 = 매출액 5%↑ or 50억↑
             (상장=분기 / 비상장=연1회)
  물류·IT서비스 = 매출·매입액 5%↑ or 50억↑ (상장회사는 200억↑)

[공익법인 — 법 §29 / 령 §36]
  기준금액 = min(100억, max(5억, max(순자산총계, 기본순자산) × 5%))
  대상행위 = 소속 국내회사 주식 취득·처분 + 자금·유가증권·자산·상품용역
  기한     = 7영업일 (비상장과 동일)

[하도급대금 결제조건 — 하도급법 §13-3]                  ← 미확정
  주기     = 반기 (종료 후 45일 설) — 하도급법 고시 원문 확인 필요
```

## 부록 B. 과태료 산정 명세 (`estimate_penalty` 구현 근거)

> 출처: 공정거래법 시행령 **[별표 9]** 과태료의 부과기준(제94조제3호, 2024-08-06 개정) + 공정위 과태료 부과기준 고시 2종. **전부 원문 확인 완료 (2026-07-29)**.

### B.1 산정 구조 — 두 체계가 다르다

```
[법 §26·§29] 대규모내부거래·공익법인
  기준금액   = 기본금액 × 거래금액별 비율    (거래금액 100억 미만)
             = 기본금액                      (거래금액 100억 이상)
  부과과태료 = 기준금액 ± 임의적 조정금액

[법 §27·§28] 비상장사 중요사항·기업집단현황
  부과과태료 = 기본금액 ± 임의적 조정금액    (거래금액 비율 적용 없음)
```

### B.2 기본금액 — 별표9 제2호 **가목** (법 §26·§29)

| 이사회 의결 | 공시 | 기한 | 누락·거짓 | 과태료 |
|---|---|---|---|---|
| 거침 | 미공시 | — | — | **5,000만원** |
| 거침 | 공시 | 기한 내 | 누락·거짓을 사전통지서 발송일 전날까지 보완 | **500만원** + 기한초과 익일~보완일 **1일 10만원** (≤2,000만원) |
| 거침 | 공시 | 기한 내 | 누락·거짓 | **2,000만원** |
| 거침 | 공시 | **기한 초과** | 없음(보완 포함) | **500만원** + **1일 10만원** (≤5,000만원) |
| 거침 | 공시 | 기한 초과 | 누락·거짓 | **5,000만원** |
| **미거침** | **미공시** | — | — | **7,000만원** |
| 미거침 | 공시 | — | 없음 | **5,000만원** |
| 미거침 | 공시 | — | 누락·거짓 | **7,000만원** |

> 자본금·자본총계 중 큰 금액이 **50억원 이하**면 기본금액은 그 금액의 **1%를 초과할 수 없다**.

### B.3 기본금액 — 별표9 제2호 **나목** (법 §27·§28)

| 공시 | 기한 | 누락·거짓 | 과태료 |
|---|---|---|---|
| 미공시 | — | — | **1,000만원** |
| 공시 | 기한 내 | 누락·거짓 보완 | **100만원** + **1일 5만원** (≤500만원) |
| 공시 | 기한 내 | 누락·거짓 | **500만원** |
| 공시 | **기한 초과** | 없음(보완 포함) | **100만원** + **1일 5만원** (≤1,000만원) |
| 공시 | 기한 초과 | 누락·거짓 | **1,000만원** |

> 자본금·자본총계 중 큰 금액이 **10억원 이하**면 기본금액은 그 금액의 **1%를 초과할 수 없다**.

### B.4 가중 (기본금액의 1/2 한도)

| 사유 | 비율 | 적용 |
|---|---|---|
| **공시의무 회피 목적 고의 분할거래** | **+50%** | 공통 |
| 최근 5개년(점검연도 포함) 위반 4~6회 | +10% (점검연도 전체 과태료) | 공통 |
| 최근 5개년 위반 7회 이상 | +20% | 공통 |

> §27·§28은 비상장사 중요사항공시와 기업집단현황공시에 **각각 별도 적용**한다.

### B.5 감경 (기본금액의 3/4 한도, 체납자 제외)

| 사유 | 비율 | 적용 |
|---|---|---|
| **공시지연 3일 이하** | **-75%** | 공통 |
| **공시지연 7일 이하** | **-50%** | 공통 |
| **공시지연 15일 이하** | **-30%** | 공통 |
| **공시지연 30일 이하** | **-20%** | 공통 |
| 신규 지정·편입일 후 30일 이내 위반 | -50% | 공통 |
| 계약 동일성 유지 + 기간 자동연장 | -30% | 공통 |
| 최초 위반 또는 최근 5개년 무위반 | -20% | §27·§28 |
| 공시주체의 적극적 행위 없는 지분율 변동 | -20% | §27·§28 |
| 계열 금융투자회사의 사실상 중개, 매도·매수인 비계열 확인 | -40% | §26·§29 |
| 민간투자법 §14 민간투자사업자 | -50% | §26·§29 |

> ⚠️ 위반 정도가 경미한 사유(신규편입 30일 / 지연일수)는 **"해당 비율 중 큰 하나만 적용"**한다. 중복 합산이 아니다.

### B.6 면제 사유

- 공시기한 만료일 **다음 날부터 10영업일 이내에 자진 시정·재공시** + 다음 중 하나
  - 신규 지정·편입 **통지일부터 30일 이내**에 공시한 경우
  - 단순 계산실수·오기 등 사소한 부주의로, **다른 공시를 통해 사실내용이 확인**되는 경우
  - (§27·§28 한정) 특수관계인 거래현황 중 **5억원 미만** 상품·용역 거래
- 명칭·성명·날짜·금액 등 **단순 누락이나 명백한 오류**로 오인 가능성이 거의 없는 경우
- 전산장애·천재지변 등 불가항력
- ※ 과태료 **체납 중인 자는 감경·면제 모두 배제**

### B.7 상한

```
부과 과태료 합계 ≤ min(자본금·자본총계 중 큰 금액 × 10%,  10억원)
1만원 단위 미만은 절사
```

### B.8 미확보 항목

거래금액 100억 미만에 적용하는 **거래금액별 비율표**(대규모내부거래 과태료 고시 Ⅵ.2)가 고시에 **이미지로만** 실려 있다. 구현 전 공정위 원문(HWP) 확보 필요.

### B.9 실무 시사점

지연일수 감경 구간이 **3일 / 7일 / 15일 / 30일**로 계단식이다. 하루 차이로 감경률이 75%→50%로 떨어지는 지점이 있어, **"지금 공시하면 얼마, 3일 뒤면 얼마"**를 보여주는 것만으로도 담당자에게 즉각적 가치가 된다. `estimate_penalty`는 이 구간 경계를 반드시 함께 제시한다.

---

## 부록 C. 참고 자료

**경쟁·벤치마크**
- [chrisryugj/korean-law-mcp](https://github.com/chrisryugj/korean-law-mcp) — 벤치마크 (2.3k★)
- [chrisryugj/korean-dart-mcp](https://github.com/chrisryugj/korean-dart-mcp) — 최강 경쟁자 (87★)
- [snaiws/DART-mcp-server](https://github.com/snaiws/DART-mcp-server)
- [keonho-kim/OpenDart-mcp](https://github.com/keonho-kim/OpenDart-mcp)
- [SongHyojun0228/opendart-mcp-server](https://github.com/SongHyojun0228/opendart-mcp-server)
- [darjeeling/awesome-mcp-korea](https://github.com/darjeeling/awesome-mcp-korea) — 등재 목표

**API**
- [OpenDART 개발가이드](https://opendart.fss.or.kr/guide/main.do?apiGrpCd=DS001)
- [공시검색 API 상세](https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS001&apiId=2019001)
- [기업집단포털 OpenAPI](https://www.egroup.go.kr/egps/wi/site/infoApiPage.do)
- [공공데이터포털 — 소속회사 조회](https://www.data.go.kr/data/15091891/openapi.do)

**법령·정책**
- 독점규제 및 공정거래에 관한 법률 시행령 (법령ID 003440, 2026-03-24 시행) §33·§34·§35·§36 — 법제처 원문 확인
- [2025년 공시이행 점검결과](https://www.korea.kr/briefing/pressReleaseView.do?newsId=156737068)
- [2026년 공시대상기업집단 지정 결과](https://www.korea.kr/briefing/pressReleaseView.do?newsId=156759013)
- [대규모기업집단현황 공시제도 실무 매뉴얼](https://www.korea.kr/archive/expDocView.do?docId=29022)
